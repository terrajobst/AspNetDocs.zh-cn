---
uid: mvc/overview/older-versions-1/models-data/creating-model-classes-with-linq-to-sql-cs
title: 用 LINQ to SQL （C#）创建模型类 |Microsoft Docs
author: microsoft
description: 本教程的目的是介绍为 ASP.NET MVC 应用程序创建模型类的一种方法。 在本教程中，您将了解如何生成模型 c 。
ms.author: riande
ms.date: 10/07/2008
ms.assetid: f84b4a16-e8bb-49e8-87a0-1832879a3501
msc.legacyurl: /mvc/overview/older-versions-1/models-data/creating-model-classes-with-linq-to-sql-cs
msc.type: authoredcontent
ms.openlocfilehash: c27d1ffac3846fe4bc13b32c2ae91a63b2493126
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 11/28/2019
ms.locfileid: "74590224"
---
# <a name="creating-model-classes-with-linq-to-sql-c"></a><span data-ttu-id="834c1-104">使用 LINQ to SQL 创建模型类 (C#)</span><span class="sxs-lookup"><span data-stu-id="834c1-104">Creating Model Classes with LINQ to SQL (C#)</span></span>

<span data-ttu-id="834c1-105">由[Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="834c1-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="834c1-106">下载 PDF</span><span class="sxs-lookup"><span data-stu-id="834c1-106">Download PDF</span></span>](https://download.microsoft.com/download/1/1/f/11f721aa-d749-4ed7-bb89-a681b68894e6/ASPNET_MVC_Tutorial_10_CS.pdf)

> <span data-ttu-id="834c1-107">本教程的目的是介绍为 ASP.NET MVC 应用程序创建模型类的一种方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-107">The goal of this tutorial is to explain one method of creating model classes for an ASP.NET MVC application.</span></span> <span data-ttu-id="834c1-108">在本教程中，您将了解如何通过利用 Microsoft LINQ to SQL 来生成模型类并执行数据库访问。</span><span class="sxs-lookup"><span data-stu-id="834c1-108">In this tutorial, you learn how to build model classes and perform database access by taking advantage of Microsoft LINQ to SQL.</span></span>

<span data-ttu-id="834c1-109">本教程的目的是介绍为 ASP.NET MVC 应用程序创建模型类的一种方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-109">The goal of this tutorial is to explain one method of creating model classes for an ASP.NET MVC application.</span></span> <span data-ttu-id="834c1-110">在本教程中，您将了解如何通过利用 Microsoft LINQ to SQL 来生成模型类并执行数据库访问</span><span class="sxs-lookup"><span data-stu-id="834c1-110">In this tutorial, you learn how to build model classes and perform database access by taking advantage of Microsoft LINQ to SQL</span></span>

<span data-ttu-id="834c1-111">在本教程中，我们将构建一个基本的电影数据库应用程序。</span><span class="sxs-lookup"><span data-stu-id="834c1-111">In this tutorial, we build a basic Movie database application.</span></span> <span data-ttu-id="834c1-112">首先，我们以尽可能快、最简单的方式创建电影数据库应用程序。</span><span class="sxs-lookup"><span data-stu-id="834c1-112">We start by creating the Movie database application in the fastest and easiest way possible.</span></span> <span data-ttu-id="834c1-113">我们直接从控制器操作执行所有数据访问。</span><span class="sxs-lookup"><span data-stu-id="834c1-113">We perform all of our data access directly from our controller actions.</span></span>

<span data-ttu-id="834c1-114">接下来，了解如何使用存储库模式。</span><span class="sxs-lookup"><span data-stu-id="834c1-114">Next, you learn how to use the Repository pattern.</span></span> <span data-ttu-id="834c1-115">使用存储库模式需要一些更多的工作。</span><span class="sxs-lookup"><span data-stu-id="834c1-115">Using the Repository pattern requires a little more work.</span></span> <span data-ttu-id="834c1-116">但是，采用此模式的优点是，它使你能够构建可适应变化并可轻松测试的应用程序。</span><span class="sxs-lookup"><span data-stu-id="834c1-116">However, the advantage of adopting this pattern is that it enables you to build applications that are adaptable to change and can be easily tested.</span></span>

## <a name="what-is-a-model-class"></a><span data-ttu-id="834c1-117">什么是模型类？</span><span class="sxs-lookup"><span data-stu-id="834c1-117">What is a Model Class?</span></span>

<span data-ttu-id="834c1-118">MVC 模型包含未包含在 MVC 视图或 MVC 控制器中的所有应用程序逻辑。</span><span class="sxs-lookup"><span data-stu-id="834c1-118">An MVC model contains all of the application logic that is not contained in an MVC view or MVC controller.</span></span> <span data-ttu-id="834c1-119">特别是，MVC 模型包含所有应用程序业务和数据访问逻辑。</span><span class="sxs-lookup"><span data-stu-id="834c1-119">In particular, an MVC model contains all of your application business and data access logic.</span></span>

<span data-ttu-id="834c1-120">您可以使用各种不同的技术来实现您的数据访问逻辑。</span><span class="sxs-lookup"><span data-stu-id="834c1-120">You can use a variety of different technologies to implement your data access logic.</span></span> <span data-ttu-id="834c1-121">例如，可以使用 Microsoft 实体框架、NHibernate、Subsonic 或 ADO.NET 类生成数据访问类。</span><span class="sxs-lookup"><span data-stu-id="834c1-121">For example, you can build your data access classes using the Microsoft Entity Framework, NHibernate, Subsonic, or ADO.NET classes.</span></span>

<span data-ttu-id="834c1-122">在本教程中，我使用 LINQ to SQL 来查询和更新数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-122">In this tutorial, I use LINQ to SQL to query and update the database.</span></span> <span data-ttu-id="834c1-123">LINQ to SQL 提供了一种非常简单的方法与 Microsoft SQL Server 数据库交互。</span><span class="sxs-lookup"><span data-stu-id="834c1-123">LINQ to SQL provides you with a very easy method of interacting with a Microsoft SQL Server database.</span></span> <span data-ttu-id="834c1-124">但是，请务必了解，ASP.NET MVC 框架不会以任何方式关联到 LINQ to SQL。</span><span class="sxs-lookup"><span data-stu-id="834c1-124">However, it is important to understand that the ASP.NET MVC framework is not tied to LINQ to SQL in any way.</span></span> <span data-ttu-id="834c1-125">ASP.NET MVC 与任何数据访问技术兼容。</span><span class="sxs-lookup"><span data-stu-id="834c1-125">ASP.NET MVC is compatible with any data access technology.</span></span>

## <a name="create-a-movie-database"></a><span data-ttu-id="834c1-126">创建电影数据库</span><span class="sxs-lookup"><span data-stu-id="834c1-126">Create a Movie Database</span></span>

<span data-ttu-id="834c1-127">在本教程中，为了说明如何生成模型类，我们构建了一个简单的电影数据库应用程序。</span><span class="sxs-lookup"><span data-stu-id="834c1-127">In this tutorial -- in order to illustrate how you can build model classes -- we build a simple Movie database application.</span></span> <span data-ttu-id="834c1-128">第一步是创建一个新数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-128">The first step is to create a new database.</span></span> <span data-ttu-id="834c1-129">右键单击 "解决方案资源管理器" 窗口中的 "应用\_Data" 文件夹，然后选择 "**添加"、"新建项**" 菜单。</span><span class="sxs-lookup"><span data-stu-id="834c1-129">Right-click the App\_Data folder in the Solution Explorer window and select the menu option **Add, New Item**.</span></span> <span data-ttu-id="834c1-130">选择**SQL Server 的数据库**模板，为其指定名称 MoviesDB，然后单击 "**添加**" 按钮（请参阅图1）。</span><span class="sxs-lookup"><span data-stu-id="834c1-130">Select the **SQL Server Database** template, give it the name MoviesDB.mdf, and click the **Add** button (see Figure 1).</span></span>

<span data-ttu-id="834c1-131">[![添加新的 SQL Server 数据库](creating-model-classes-with-linq-to-sql-cs/_static/image2.png)](creating-model-classes-with-linq-to-sql-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-131">[![Adding a new SQL Server Database](creating-model-classes-with-linq-to-sql-cs/_static/image2.png)](creating-model-classes-with-linq-to-sql-cs/_static/image1.png)</span></span>

<span data-ttu-id="834c1-132">**图 01**：添加新的 SQL Server 数据库（[单击以查看完全大小的映像](creating-model-classes-with-linq-to-sql-cs/_static/image3.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-132">**Figure 01**: Adding a new SQL Server Database ([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image3.png))</span></span>

<span data-ttu-id="834c1-133">创建新数据库后，可以通过双击应用\_Data 文件夹中的 MoviesDB 文件打开数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-133">After you create the new database, you can open the database by double-clicking the MoviesDB.mdf file in the App\_Data folder.</span></span> <span data-ttu-id="834c1-134">双击 MoviesDB 文件将打开 "服务器资源管理器" 窗口（参见图2）。</span><span class="sxs-lookup"><span data-stu-id="834c1-134">Double-clicking the MoviesDB.mdf file opens the Server Explorer window (see Figure 2).</span></span>

<span data-ttu-id="834c1-135">使用 Visual Web Developer 时，"服务器资源管理器" 窗口称为 "数据库资源管理器" 窗口。</span><span class="sxs-lookup"><span data-stu-id="834c1-135">The Server Explorer window is called the Database Explorer window when using Visual Web Developer.</span></span>

<span data-ttu-id="834c1-136">[使用服务器资源管理器窗口 ![](creating-model-classes-with-linq-to-sql-cs/_static/image5.png)](creating-model-classes-with-linq-to-sql-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-136">[![Using the Server Explorer window](creating-model-classes-with-linq-to-sql-cs/_static/image5.png)](creating-model-classes-with-linq-to-sql-cs/_static/image4.png)</span></span>

<span data-ttu-id="834c1-137">**图 02**：使用服务器资源管理器窗口（[单击以查看完全大小的图像](creating-model-classes-with-linq-to-sql-cs/_static/image6.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-137">**Figure 02**: Using the Server Explorer window ([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image6.png))</span></span>

<span data-ttu-id="834c1-138">我们需要将一个表添加到表示电影的数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-138">We need to add one table to our database that represents our movies.</span></span> <span data-ttu-id="834c1-139">右键单击 "表" 文件夹，然后选择 "**添加新表**" 菜单选项。</span><span class="sxs-lookup"><span data-stu-id="834c1-139">Right-click the Tables folder and select the menu option **Add New Table**.</span></span> <span data-ttu-id="834c1-140">选择此菜单选项将打开表设计器（请参阅图3）。</span><span class="sxs-lookup"><span data-stu-id="834c1-140">Selecting this menu option opens the Table Designer (see Figure 3).</span></span>

<span data-ttu-id="834c1-141">[使用服务器资源管理器窗口 ![](creating-model-classes-with-linq-to-sql-cs/_static/image8.png)](creating-model-classes-with-linq-to-sql-cs/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-141">[![Using the Server Explorer window](creating-model-classes-with-linq-to-sql-cs/_static/image8.png)](creating-model-classes-with-linq-to-sql-cs/_static/image7.png)</span></span>

<span data-ttu-id="834c1-142">**图 03**：表设计器（[单击查看完全尺寸的图像](creating-model-classes-with-linq-to-sql-cs/_static/image9.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-142">**Figure 03**: The Table Designer ([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image9.png))</span></span>

<span data-ttu-id="834c1-143">我们需要在数据库表中添加以下列：</span><span class="sxs-lookup"><span data-stu-id="834c1-143">We need to add the following columns to our database table:</span></span>

| <span data-ttu-id="834c1-144">**列名**</span><span class="sxs-lookup"><span data-stu-id="834c1-144">**Column Name**</span></span> | <span data-ttu-id="834c1-145">**数据类型**</span><span class="sxs-lookup"><span data-stu-id="834c1-145">**Data Type**</span></span> | <span data-ttu-id="834c1-146">**允许空值**</span><span class="sxs-lookup"><span data-stu-id="834c1-146">**Allow Nulls**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="834c1-147">Id</span><span class="sxs-lookup"><span data-stu-id="834c1-147">Id</span></span> | <span data-ttu-id="834c1-148">Int</span><span class="sxs-lookup"><span data-stu-id="834c1-148">Int</span></span> | <span data-ttu-id="834c1-149">False</span><span class="sxs-lookup"><span data-stu-id="834c1-149">False</span></span> |
| <span data-ttu-id="834c1-150">职务</span><span class="sxs-lookup"><span data-stu-id="834c1-150">Title</span></span> | <span data-ttu-id="834c1-151">Nvarchar （200）</span><span class="sxs-lookup"><span data-stu-id="834c1-151">Nvarchar(200)</span></span> | <span data-ttu-id="834c1-152">False</span><span class="sxs-lookup"><span data-stu-id="834c1-152">False</span></span> |
| <span data-ttu-id="834c1-153">9509</span><span class="sxs-lookup"><span data-stu-id="834c1-153">Director</span></span> | <span data-ttu-id="834c1-154">Nvarchar （50）</span><span class="sxs-lookup"><span data-stu-id="834c1-154">Nvarchar(50)</span></span> | <span data-ttu-id="834c1-155">False</span><span class="sxs-lookup"><span data-stu-id="834c1-155">False</span></span> |

<span data-ttu-id="834c1-156">需要为 Id 列执行两个特殊操作。</span><span class="sxs-lookup"><span data-stu-id="834c1-156">You need to do two special things to the Id column.</span></span> <span data-ttu-id="834c1-157">首先，您需要通过在表设计器中选择列并单击键的图标，将 Id 列标记为主键列。</span><span class="sxs-lookup"><span data-stu-id="834c1-157">First, you need to mark the Id column as a primary key column by selecting the column in the Table Designer and clicking the icon of a key.</span></span> <span data-ttu-id="834c1-158">LINQ to SQL 要求在对数据库执行插入或更新时指定主键列。</span><span class="sxs-lookup"><span data-stu-id="834c1-158">LINQ to SQL requires you to specify your primary key columns when performing inserts or updates against the database.</span></span>

<span data-ttu-id="834c1-159">接下来，需要通过将 "是" 值指定为 "**是" 标识**属性，将 Id 列标记为标识列（请参阅图3）。</span><span class="sxs-lookup"><span data-stu-id="834c1-159">Next, you need to mark the Id column as an Identity column by assigning the value Yes to the **Is Identity** property (see Figure 3).</span></span> <span data-ttu-id="834c1-160">标识列是在向表中添加新的数据行时，将自动分配新数字的列。</span><span class="sxs-lookup"><span data-stu-id="834c1-160">An Identity column is a column that is assigned a new number automatically whenever you add a new row of data to a table.</span></span>

## <a name="create-linq-to-sql-classes"></a><span data-ttu-id="834c1-161">创建 LINQ to SQL 类</span><span class="sxs-lookup"><span data-stu-id="834c1-161">Create LINQ to SQL Classes</span></span>

<span data-ttu-id="834c1-162">MVC 模型将包含表示 tblMovie 数据库表的 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-162">Our MVC model will contain LINQ to SQL classes that represent the tblMovie database table.</span></span> <span data-ttu-id="834c1-163">创建这些 LINQ to SQL 类的最简单方法是：右键单击 "模型" 文件夹，选择 "**添加"、"新建项**"，选择 "LINQ to SQL 类" 模板，为类命名为 "Movie"，然后单击 "**添加**" 按钮（见图4）。</span><span class="sxs-lookup"><span data-stu-id="834c1-163">The easiest way to create these LINQ to SQL classes is to right-click the Models folder, select **Add, New Item**, select the LINQ to SQL Classes template, give the classes the name Movie.dbml, and click the **Add** button (see Figure 4).</span></span>

<span data-ttu-id="834c1-164">[![创建 LINQ to SQL 类](creating-model-classes-with-linq-to-sql-cs/_static/image11.png)](creating-model-classes-with-linq-to-sql-cs/_static/image10.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-164">[![Creating LINQ to SQL classes](creating-model-classes-with-linq-to-sql-cs/_static/image11.png)](creating-model-classes-with-linq-to-sql-cs/_static/image10.png)</span></span>

<span data-ttu-id="834c1-165">**图 04**：创建 LINQ to SQL 类（[单击以查看完全大小的图像](creating-model-classes-with-linq-to-sql-cs/_static/image12.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-165">**Figure 04**: Creating LINQ to SQL classes ([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image12.png))</span></span>

<span data-ttu-id="834c1-166">LINQ to SQL 类创建影片后，会立即显示对象关系设计器。</span><span class="sxs-lookup"><span data-stu-id="834c1-166">Immediately after you create the Movie LINQ to SQL Classes, the Object Relational Designer appears.</span></span> <span data-ttu-id="834c1-167">您可以将数据库表从 "服务器资源管理器" 窗口拖到 "对象关系设计器上，以创建表示特定数据库表 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-167">You can drag database tables from the Server Explorer window onto the Object Relational Designer to create LINQ to SQL Classes that represent particular database tables.</span></span> <span data-ttu-id="834c1-168">需要将 tblMovie 数据库表添加到对象关系设计器（请参阅图5）。</span><span class="sxs-lookup"><span data-stu-id="834c1-168">We need to add the tblMovie database table onto the Object Relational Designer (see Figure 5).</span></span>

<span data-ttu-id="834c1-169">[使用对象关系设计器 ![](creating-model-classes-with-linq-to-sql-cs/_static/image14.png)](creating-model-classes-with-linq-to-sql-cs/_static/image13.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-169">[![Using the Object Relational Designer](creating-model-classes-with-linq-to-sql-cs/_static/image14.png)](creating-model-classes-with-linq-to-sql-cs/_static/image13.png)</span></span>

<span data-ttu-id="834c1-170">**图 05**：使用对象关系设计器（[单击查看完全尺寸的图像](creating-model-classes-with-linq-to-sql-cs/_static/image15.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-170">**Figure 05**: Using the Object Relational Designer  ([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image15.png))</span></span>

<span data-ttu-id="834c1-171">默认情况下，对象关系设计器会创建一个与拖到设计器上的数据库表同名的类。</span><span class="sxs-lookup"><span data-stu-id="834c1-171">By default, the Object Relational Designer creates a class with the very same name as the database table that you drag onto the Designer.</span></span> <span data-ttu-id="834c1-172">但是，我们不想要 `tblMovie`的类。</span><span class="sxs-lookup"><span data-stu-id="834c1-172">However, we don't want to call our class `tblMovie`.</span></span> <span data-ttu-id="834c1-173">因此，在设计器中单击类的名称，并将类的名称更改为 "电影"。</span><span class="sxs-lookup"><span data-stu-id="834c1-173">Therefore, click the name of the class in the Designer and change the name of the class to Movie.</span></span>

<span data-ttu-id="834c1-174">最后，请记住单击 "**保存**" 按钮（软盘的图片）以保存 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-174">Finally, remember to click the **Save** button (the picture of the floppy) to save the LINQ to SQL Classes.</span></span> <span data-ttu-id="834c1-175">否则，对象关系设计器不会生成 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-175">Otherwise, the LINQ to SQL Classes won't be generated by the Object Relational Designer.</span></span>

## <a name="using-linq-to-sql-in-a-controller-action"></a><span data-ttu-id="834c1-176">在控制器操作中使用 LINQ to SQL</span><span class="sxs-lookup"><span data-stu-id="834c1-176">Using LINQ to SQL in a Controller Action</span></span>

<span data-ttu-id="834c1-177">现在我们有了 LINQ to SQL 类，可以使用这些类从数据库中检索数据。</span><span class="sxs-lookup"><span data-stu-id="834c1-177">Now that we have our LINQ to SQL classes, we can use these classes to retrieve data from the database.</span></span> <span data-ttu-id="834c1-178">本部分介绍如何直接在控制器操作中使用 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-178">In this section, you learn how to use LINQ to SQL classes directly within a controller action.</span></span> <span data-ttu-id="834c1-179">我们将在 MVC 视图中的 tblMovies 数据库表中显示电影列表。</span><span class="sxs-lookup"><span data-stu-id="834c1-179">We'll display the list of movies from the tblMovies database table in an MVC view.</span></span>

<span data-ttu-id="834c1-180">首先，我们需要修改 HomeController 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-180">First, we need to modify the HomeController class.</span></span> <span data-ttu-id="834c1-181">此类可在应用程序的 "控制器" 文件夹中找到。</span><span class="sxs-lookup"><span data-stu-id="834c1-181">This class can be found in the Controllers folder of your application.</span></span> <span data-ttu-id="834c1-182">修改类，使其类似于列表1中的类。</span><span class="sxs-lookup"><span data-stu-id="834c1-182">Modify the class so it looks like the class in Listing 1.</span></span>

<span data-ttu-id="834c1-183">**列表1– `Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="834c1-183">**Listing 1 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](creating-model-classes-with-linq-to-sql-cs/samples/sample1.cs)]

<span data-ttu-id="834c1-184">列表1中的 `Index()` 操作使用 LINQ to SQL DataContext 类（`MovieDataContext`）来表示 `MoviesDB` 数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-184">The `Index()` action in Listing 1 uses a LINQ to SQL DataContext class (the `MovieDataContext`) to represent the `MoviesDB` database.</span></span> <span data-ttu-id="834c1-185">`MoveDataContext` 类是由 Visual Studio 对象关系设计器生成的。</span><span class="sxs-lookup"><span data-stu-id="834c1-185">The `MoveDataContext` class was generated by the Visual Studio Object Relational Designer.</span></span>

<span data-ttu-id="834c1-186">针对 DataContext 执行 LINQ 查询，以检索 `tblMovies` 数据库表中的所有电影。</span><span class="sxs-lookup"><span data-stu-id="834c1-186">A LINQ query is performed against the DataContext to retrieve all of the movies from the `tblMovies` database table.</span></span> <span data-ttu-id="834c1-187">电影列表被分配给名为 `movies`的局部变量。</span><span class="sxs-lookup"><span data-stu-id="834c1-187">The list of movies is assigned to a local variable named `movies`.</span></span> <span data-ttu-id="834c1-188">最后，将电影列表通过视图数据传递给视图。</span><span class="sxs-lookup"><span data-stu-id="834c1-188">Finally, the list of movies is passed to the view through view data.</span></span>

<span data-ttu-id="834c1-189">为了显示电影，接下来需要修改索引视图。</span><span class="sxs-lookup"><span data-stu-id="834c1-189">In order to show the movies, we next need to modify the Index view.</span></span> <span data-ttu-id="834c1-190">可以在 "`Views\Home\`" 文件夹中找到索引视图。</span><span class="sxs-lookup"><span data-stu-id="834c1-190">You can find the Index view in the `Views\Home\` folder.</span></span> <span data-ttu-id="834c1-191">更新 "索引" 视图，使其看起来像列表2中的视图。</span><span class="sxs-lookup"><span data-stu-id="834c1-191">Update the Index view so that it looks like the view in Listing 2.</span></span>

<span data-ttu-id="834c1-192">**列表2– `Views\Home\Index.aspx`**</span><span class="sxs-lookup"><span data-stu-id="834c1-192">**Listing 2 – `Views\Home\Index.aspx`**</span></span>

[!code-aspx[Main](creating-model-classes-with-linq-to-sql-cs/samples/sample2.aspx)]

<span data-ttu-id="834c1-193">请注意，修改后的索引视图在视图顶部包含一个 `<%@ import namespace %>` 指令。</span><span class="sxs-lookup"><span data-stu-id="834c1-193">Notice that the modified Index view includes an `<%@ import namespace %>` directive at the top of the view.</span></span> <span data-ttu-id="834c1-194">此指令将导入 `MvcApplication1.Models namespace`。</span><span class="sxs-lookup"><span data-stu-id="834c1-194">This directive imports the `MvcApplication1.Models namespace`.</span></span> <span data-ttu-id="834c1-195">需要使用此命名空间才能处理 `model` 类（特别是视图中的 `Movie` 类）。</span><span class="sxs-lookup"><span data-stu-id="834c1-195">We need this namespace in order to work with the `model` classes – in particular, the `Movie` class -- in the view.</span></span>

<span data-ttu-id="834c1-196">清单2中的视图包含一个 `foreach` 循环，该循环遍历 `ViewData.Model` 属性表示的所有项。</span><span class="sxs-lookup"><span data-stu-id="834c1-196">The view in Listing 2 contains a `foreach` loop that iterates through all of the items represented by the `ViewData.Model` property.</span></span> <span data-ttu-id="834c1-197">为每个 `movie`显示 `Title` 属性的值。</span><span class="sxs-lookup"><span data-stu-id="834c1-197">The value of the `Title` property is displayed for each `movie`.</span></span>

<span data-ttu-id="834c1-198">请注意，`ViewData.Model` 属性的值强制转换为 `IEnumerable`。</span><span class="sxs-lookup"><span data-stu-id="834c1-198">Notice that the value of the `ViewData.Model` property is cast to an `IEnumerable`.</span></span> <span data-ttu-id="834c1-199">为了遍历 `ViewData.Model`的内容，这是必需的。</span><span class="sxs-lookup"><span data-stu-id="834c1-199">This is necessary in order to loop through the contents of `ViewData.Model`.</span></span> <span data-ttu-id="834c1-200">此处的另一个选项是创建强类型 `view`。</span><span class="sxs-lookup"><span data-stu-id="834c1-200">Another option here is to create a strongly-typed `view`.</span></span> <span data-ttu-id="834c1-201">创建强类型 `view`时，将 `ViewData.Model` 属性强制转换为视图的代码隐藏类中的特定类型。</span><span class="sxs-lookup"><span data-stu-id="834c1-201">When you create a strongly-typed `view`, you cast the `ViewData.Model` property to a particular type in a view's code-behind class.</span></span>

<span data-ttu-id="834c1-202">如果在修改 `HomeController` 类和索引视图后运行该应用程序，则会看到一个空白页。</span><span class="sxs-lookup"><span data-stu-id="834c1-202">If you run the application after modifying the `HomeController` class and the Index view then you will get a blank page.</span></span> <span data-ttu-id="834c1-203">您将看到一个空白页，因为 `tblMovies` 数据库表中没有电影记录。</span><span class="sxs-lookup"><span data-stu-id="834c1-203">You'll get a blank page because there are no movie records in the `tblMovies` database table.</span></span>

<span data-ttu-id="834c1-204">若要将记录添加到 `tblMovies` 数据库表，请在 "服务器资源管理器" 窗口（Visual Web Developer 中的数据库资源管理器窗口）中右键单击 `tblMovies` 数据库表，然后选择菜单选项 "显示表数据"。</span><span class="sxs-lookup"><span data-stu-id="834c1-204">In order to add records to the `tblMovies` database table, right-click the `tblMovies` database table in the Server Explorer window (Database Explorer window in Visual Web Developer) and select the menu option Show Table Data.</span></span> <span data-ttu-id="834c1-205">您可以使用显示的网格插入 `movie` 记录（见图6）。</span><span class="sxs-lookup"><span data-stu-id="834c1-205">You can insert `movie` records by using the grid that appears (see Figure 6).</span></span>

<span data-ttu-id="834c1-206">[![插入电影](creating-model-classes-with-linq-to-sql-cs/_static/image17.png)](creating-model-classes-with-linq-to-sql-cs/_static/image16.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-206">[![Inserting movies](creating-model-classes-with-linq-to-sql-cs/_static/image17.png)](creating-model-classes-with-linq-to-sql-cs/_static/image16.png)</span></span>

<span data-ttu-id="834c1-207">**图 06**：插入影片（[单击以查看完全尺寸的图像](creating-model-classes-with-linq-to-sql-cs/_static/image18.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-207">**Figure 06**: Inserting movies([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image18.png))</span></span>

<span data-ttu-id="834c1-208">将一些数据库记录添加到 `tblMovies` 表中，并运行该应用程序后，将看到图7中的页面。</span><span class="sxs-lookup"><span data-stu-id="834c1-208">After you add some database records to the `tblMovies` table, and you run the application, you'll see the page in Figure 7.</span></span> <span data-ttu-id="834c1-209">所有电影数据库记录都显示在项目符号列表中。</span><span class="sxs-lookup"><span data-stu-id="834c1-209">All of the movie database records are displayed in a bulleted list.</span></span>

<span data-ttu-id="834c1-210">[用索引视图 ![显示电影](creating-model-classes-with-linq-to-sql-cs/_static/image20.png)](creating-model-classes-with-linq-to-sql-cs/_static/image19.png)</span><span class="sxs-lookup"><span data-stu-id="834c1-210">[![Displaying movies with the Index view](creating-model-classes-with-linq-to-sql-cs/_static/image20.png)](creating-model-classes-with-linq-to-sql-cs/_static/image19.png)</span></span>

<span data-ttu-id="834c1-211">**图 07**：通过 "索引" 视图显示电影（[单击以查看完全大小的图像](creating-model-classes-with-linq-to-sql-cs/_static/image21.png)）</span><span class="sxs-lookup"><span data-stu-id="834c1-211">**Figure 07**: Displaying movies with the Index view([Click to view full-size image](creating-model-classes-with-linq-to-sql-cs/_static/image21.png))</span></span>

## <a name="using-the-repository-pattern"></a><span data-ttu-id="834c1-212">使用存储库模式</span><span class="sxs-lookup"><span data-stu-id="834c1-212">Using the Repository Pattern</span></span>

<span data-ttu-id="834c1-213">在上一部分中，我们直接在控制器操作中使用 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-213">In the previous section, we used LINQ to SQL classes directly within a controller action.</span></span> <span data-ttu-id="834c1-214">我们直接从 `Index()` 控制器操作使用了 `MovieDataContext` 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-214">We used the `MovieDataContext` class directly from the `Index()` controller action.</span></span> <span data-ttu-id="834c1-215">对于简单的应用程序，在执行此操作时没有任何错误。</span><span class="sxs-lookup"><span data-stu-id="834c1-215">There is nothing wrong with doing this in the case of a simple application.</span></span> <span data-ttu-id="834c1-216">但是，当你需要生成更复杂的应用程序时，直接在 controller 类中处理 LINQ to SQL 会产生问题。</span><span class="sxs-lookup"><span data-stu-id="834c1-216">However, working directly with LINQ to SQL in a controller class creates problems when you need to build a more complex application.</span></span>

<span data-ttu-id="834c1-217">使用控制器类中的 LINQ to SQL 使得将来难以切换数据访问技术。</span><span class="sxs-lookup"><span data-stu-id="834c1-217">Using LINQ to SQL within a controller class makes it difficult to switch data access technologies in the future.</span></span> <span data-ttu-id="834c1-218">例如，你可能决定从使用 Microsoft LINQ to SQL 切换为使用 Microsoft 实体框架作为你的数据访问技术。</span><span class="sxs-lookup"><span data-stu-id="834c1-218">For example, you might decide to switch from using Microsoft LINQ to SQL to using the Microsoft Entity Framework as your data access technology.</span></span> <span data-ttu-id="834c1-219">在这种情况下，需要重新编写访问应用程序中的数据库的每个控制器。</span><span class="sxs-lookup"><span data-stu-id="834c1-219">In that case, you would need to rewrite every controller that accesses the database within your application.</span></span>

<span data-ttu-id="834c1-220">使用控制器类中的 LINQ to SQL 也会很难为应用程序生成单元测试。</span><span class="sxs-lookup"><span data-stu-id="834c1-220">Using LINQ to SQL within a controller class also makes it difficult to build unit tests for your application.</span></span> <span data-ttu-id="834c1-221">通常，在执行单元测试时不希望与数据库进行交互。</span><span class="sxs-lookup"><span data-stu-id="834c1-221">Normally, you do not want to interact with a database when performing unit tests.</span></span> <span data-ttu-id="834c1-222">您希望使用您的单元测试来测试您的应用程序逻辑，而不是您的数据库服务器。</span><span class="sxs-lookup"><span data-stu-id="834c1-222">You want to use your unit tests to test your application logic and not your database server.</span></span>

<span data-ttu-id="834c1-223">为了构建一个可更好地适应将来的更改并且可以更轻松地测试的 MVC 应用程序，应考虑使用存储库模式。</span><span class="sxs-lookup"><span data-stu-id="834c1-223">In order to build an MVC application that is more adaptable to future change and that can be more easily tested, you should consider using the Repository pattern.</span></span> <span data-ttu-id="834c1-224">使用存储库模式时，将创建一个单独的存储库类，其中包含所有数据库访问逻辑。</span><span class="sxs-lookup"><span data-stu-id="834c1-224">When you use the Repository pattern, you create a separate repository class that contains all of your database access logic.</span></span>

<span data-ttu-id="834c1-225">创建存储库类时，将创建一个接口，该接口表示存储库类使用的所有方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-225">When you create the repository class, you create an interface that represents all of the methods used by the repository class.</span></span> <span data-ttu-id="834c1-226">在控制器中，可以针对接口而不是存储库来编写代码。</span><span class="sxs-lookup"><span data-stu-id="834c1-226">Within your controllers, you write your code against the interface instead of the repository.</span></span> <span data-ttu-id="834c1-227">这样，以后就可以使用不同的数据访问技术来实现存储库。</span><span class="sxs-lookup"><span data-stu-id="834c1-227">That way, you can implement the repository using different data access technologies in the future.</span></span>

<span data-ttu-id="834c1-228">清单3中的接口名为 `IMovieRepository`，它表示一个名为 `ListAll()`的方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-228">The interface in Listing 3 is named `IMovieRepository` and it represents a single method named `ListAll()`.</span></span>

<span data-ttu-id="834c1-229">**列表 3-`Models\IMovieRepository.cs`**</span><span class="sxs-lookup"><span data-stu-id="834c1-229">**Listing 3 – `Models\IMovieRepository.cs`**</span></span>

[!code-csharp[Main](creating-model-classes-with-linq-to-sql-cs/samples/sample3.cs)]

<span data-ttu-id="834c1-230">列表4中的存储库类实现 `IMovieRepository` 接口。</span><span class="sxs-lookup"><span data-stu-id="834c1-230">The repository class in Listing 4 implements the `IMovieRepository` interface.</span></span> <span data-ttu-id="834c1-231">请注意，它包含一个名为 `ListAll()` 的方法，该方法对应于 `IMovieRepository` 接口所需的方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-231">Notice that it contains a method named `ListAll()` that corresponds to the method required by the `IMovieRepository` interface.</span></span>

<span data-ttu-id="834c1-232">**列表 4-`Models\MovieRepository.cs`**</span><span class="sxs-lookup"><span data-stu-id="834c1-232">**Listing 4 – `Models\MovieRepository.cs`**</span></span>

[!code-csharp[Main](creating-model-classes-with-linq-to-sql-cs/samples/sample4.cs)]

<span data-ttu-id="834c1-233">最后，列表5中的 `MoviesController` 类使用存储库模式。</span><span class="sxs-lookup"><span data-stu-id="834c1-233">Finally, the `MoviesController` class in Listing 5 uses the Repository pattern.</span></span> <span data-ttu-id="834c1-234">它不再直接使用 LINQ to SQL 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-234">It no longer uses LINQ to SQL classes directly.</span></span>

<span data-ttu-id="834c1-235">**列表5– `Controllers\MoviesController.cs`**</span><span class="sxs-lookup"><span data-stu-id="834c1-235">**Listing 5 – `Controllers\MoviesController.cs`**</span></span>

[!code-csharp[Main](creating-model-classes-with-linq-to-sql-cs/samples/sample5.cs)]

<span data-ttu-id="834c1-236">请注意，列表5中的 `MoviesController` 类具有两个构造函数。</span><span class="sxs-lookup"><span data-stu-id="834c1-236">Notice that the `MoviesController` class in Listing 5 has two constructors.</span></span> <span data-ttu-id="834c1-237">第一个构造函数（即无参数构造函数）在应用程序运行时调用。</span><span class="sxs-lookup"><span data-stu-id="834c1-237">The first constructor, the parameterless constructor, is called when your application is running.</span></span> <span data-ttu-id="834c1-238">此构造函数创建 `MovieRepository` 类的实例并将其传递给第二个构造函数。</span><span class="sxs-lookup"><span data-stu-id="834c1-238">This constructor creates an instance of the `MovieRepository` class and passes it to the second constructor.</span></span>

<span data-ttu-id="834c1-239">第二个构造函数具有单个参数： `IMovieRepository` 参数。</span><span class="sxs-lookup"><span data-stu-id="834c1-239">The second constructor has a single parameter: an `IMovieRepository` parameter.</span></span> <span data-ttu-id="834c1-240">此构造函数只是将参数的值分配给名为 `_repository`的类级字段。</span><span class="sxs-lookup"><span data-stu-id="834c1-240">This constructor simply assigns the value of the parameter to a class-level field named `_repository`.</span></span>

<span data-ttu-id="834c1-241">`MoviesController` 类利用称为依赖关系注入模式的软件设计模式。</span><span class="sxs-lookup"><span data-stu-id="834c1-241">The `MoviesController` class is taking advantage of a software design pattern called the Dependency Injection pattern.</span></span> <span data-ttu-id="834c1-242">具体而言，它使用称为构造函数依赖关系注入的内容。</span><span class="sxs-lookup"><span data-stu-id="834c1-242">In particular, it is using something called Constructor Dependency Injection.</span></span> <span data-ttu-id="834c1-243">若要阅读有关此模式的详细信息，请参阅以下文章： Fowler：</span><span class="sxs-lookup"><span data-stu-id="834c1-243">You can read more about this pattern by reading the following article by Martin Fowler:</span></span>

[http://martinfowler.com/articles/injection.html](http://martinfowler.com/articles/injection.html)

<span data-ttu-id="834c1-244">请注意，`MoviesController` 类中的所有代码（第一个构造函数除外）都与 `IMovieRepository` 接口（而不是实际的 `MovieRepository` 类）交互。</span><span class="sxs-lookup"><span data-stu-id="834c1-244">Notice that all of the code in the `MoviesController` class (with the exception of the first constructor) interacts with the `IMovieRepository` interface instead of the actual `MovieRepository` class.</span></span> <span data-ttu-id="834c1-245">此代码与抽象接口交互，而不是接口的具体实现。</span><span class="sxs-lookup"><span data-stu-id="834c1-245">The code interacts with an abstract interface instead of a concrete implementation of the interface.</span></span>

<span data-ttu-id="834c1-246">如果要修改应用程序使用的数据访问技术，只需使用使用备用数据库访问技术的类来实现 `IMovieRepository` 接口。</span><span class="sxs-lookup"><span data-stu-id="834c1-246">If you want to modify the data access technology used by the application then you can simply implement the `IMovieRepository` interface with a class that uses the alternative database access technology.</span></span> <span data-ttu-id="834c1-247">例如，可以创建 `EntityFrameworkMovieRepository` 类或 `SubSonicMovieRepository` 类。</span><span class="sxs-lookup"><span data-stu-id="834c1-247">For example, you could create an `EntityFrameworkMovieRepository` class or a `SubSonicMovieRepository` class.</span></span> <span data-ttu-id="834c1-248">由于控制器类基于接口进行编程，因此您可以将 `IMovieRepository` 的新实现传递到控制器类，并且类将继续工作。</span><span class="sxs-lookup"><span data-stu-id="834c1-248">Because the controller class is programmed against the interface, you can pass a new implementation of `IMovieRepository` to the controller class and the class would continue to work.</span></span>

<span data-ttu-id="834c1-249">此外，如果想要测试 `MoviesController` 类，则可以将假电影存储库类传递到 `HomeController`。</span><span class="sxs-lookup"><span data-stu-id="834c1-249">Furthermore, if you want to test the `MoviesController` class, then you can pass a fake movie repository class to the `HomeController`.</span></span> <span data-ttu-id="834c1-250">您可以使用类来实现 `IMovieRepository` 类，该类实际上不会访问数据库，而是包含 `IMovieRepository` 接口的所有所需方法。</span><span class="sxs-lookup"><span data-stu-id="834c1-250">You can implement the `IMovieRepository` class with a class that does not actually access the database but contains all of the required methods of the `IMovieRepository` interface.</span></span> <span data-ttu-id="834c1-251">这样，就可以对 `MoviesController` 类进行单元测试，而无需实际访问实际数据库。</span><span class="sxs-lookup"><span data-stu-id="834c1-251">That way, you can unit test the `MoviesController` class without actually accessing a real database.</span></span>

## <a name="summary"></a><span data-ttu-id="834c1-252">总结</span><span class="sxs-lookup"><span data-stu-id="834c1-252">Summary</span></span>

<span data-ttu-id="834c1-253">本教程的目的是演示如何通过利用 Microsoft LINQ to SQL 来创建 MVC 模型类。</span><span class="sxs-lookup"><span data-stu-id="834c1-253">The goal of this tutorial was to demonstrate how you can create MVC model classes by taking advantage of Microsoft LINQ to SQL.</span></span> <span data-ttu-id="834c1-254">我们检查了两种在 ASP.NET MVC 应用程序中显示数据库数据的策略。</span><span class="sxs-lookup"><span data-stu-id="834c1-254">We examined two strategies for displaying database data in an ASP.NET MVC application.</span></span> <span data-ttu-id="834c1-255">首先，我们创建 LINQ to SQL 类并直接在控制器操作中使用类。</span><span class="sxs-lookup"><span data-stu-id="834c1-255">First, we created LINQ to SQL classes and used the classes directly within a controller action.</span></span> <span data-ttu-id="834c1-256">使用控制器中的 LINQ to SQL 类，可以快速轻松地在 MVC 应用程序中显示数据库数据。</span><span class="sxs-lookup"><span data-stu-id="834c1-256">Using LINQ to SQL classes within a controller enables you to quickly and easily display database data in an MVC application.</span></span>

<span data-ttu-id="834c1-257">接下来，我们探讨了稍微复杂一些的良性，显示数据库数据的路径。</span><span class="sxs-lookup"><span data-stu-id="834c1-257">Next, we explored a slightly more difficult, but definitely more virtuous, path for displaying database data.</span></span> <span data-ttu-id="834c1-258">我们充分利用了存储库模式，并在单独的存储库类中放置了所有数据库访问逻辑。</span><span class="sxs-lookup"><span data-stu-id="834c1-258">We took advantage of the Repository pattern and placed all of our database access logic in a separate repository class.</span></span> <span data-ttu-id="834c1-259">在我们的控制器中，我们针对接口而不是具体的类编写了所有代码。</span><span class="sxs-lookup"><span data-stu-id="834c1-259">In our controller, we wrote all of our code against an interface instead of a concrete class.</span></span> <span data-ttu-id="834c1-260">存储库模式的优点在于，它使我们能够在以后轻松更改数据库访问技术，使我们可以轻松地测试控制器类。</span><span class="sxs-lookup"><span data-stu-id="834c1-260">The advantage of the Repository pattern is that it enables us to easily change database access technologies in the future and it enables us to easily test our controller classes.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="834c1-261">[上一页](creating-model-classes-with-the-entity-framework-cs.md)
> [下一页](displaying-a-table-of-database-data-cs.md)</span><span class="sxs-lookup"><span data-stu-id="834c1-261">[Previous](creating-model-classes-with-the-entity-framework-cs.md)
[Next](displaying-a-table-of-database-data-cs.md)</span></span>
