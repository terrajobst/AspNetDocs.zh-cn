---
uid: mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
title: 在 ASP.NET MVC 4 中使用异步方法 |Microsoft Docs
author: Rick-Anderson
description: 本教程将教你使用 Visual Studio Express 2012 for Web 构建异步 ASP.NET MVC Web 应用程序的基础知识，这是一款免费版 。
ms.author: riande
ms.date: 06/06/2012
ms.assetid: a56572ba-81c3-47af-826d-941e9c4775ec
msc.legacyurl: /mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
msc.type: authoredcontent
ms.openlocfilehash: 15692b18fc112c4c6cce4d50a243a0e8d5fb52a4
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/06/2020
ms.locfileid: "78432884"
---
# <a name="using-asynchronous-methods-in-aspnet-mvc-4"></a><span data-ttu-id="933e1-103">在 ASP.NET MVC 4 中使用异步方法</span><span class="sxs-lookup"><span data-stu-id="933e1-103">Using Asynchronous Methods in ASP.NET MVC 4</span></span>

<span data-ttu-id="933e1-104">作者： [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="933e1-104">by [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

> <span data-ttu-id="933e1-105">本教程将介绍使用[Visual Studio Express 2012 For Web](https://www.microsoft.com/visualstudio/11)构建异步 ASP.NET MVC Web 应用程序的基础知识，这是 Microsoft Visual Studio 的免费版本。</span><span class="sxs-lookup"><span data-stu-id="933e1-105">This tutorial will teach you the basics of building an asynchronous ASP.NET MVC Web application using [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), which is a free version of Microsoft Visual Studio.</span></span> <span data-ttu-id="933e1-106">你还可以使用[Visual Studio 2012](https://www.microsoft.com/visualstudio/11)。</span><span class="sxs-lookup"><span data-stu-id="933e1-106">You can also use [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span></span>
> 
> <span data-ttu-id="933e1-107">本教程在 github 上提供了完整的示例[https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span><span class="sxs-lookup"><span data-stu-id="933e1-107">A complete sample is provided for this tutorial on github [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span></span>

<span data-ttu-id="933e1-108">结合使用[.net 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx)的 ASP.NET MVC 4[控制器](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx)类，您可以编写返回 Task 类型为[Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx)的对象的异步操作方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-108">The ASP.NET MVC 4 [Controller](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) class in combination [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) enables you to write asynchronous action methods that return an object of type [Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span></span> <span data-ttu-id="933e1-109">.NET Framework 4 引入了一个称为[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)的异步编程概念，ASP.NET MVC 4 支持[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)。</span><span class="sxs-lookup"><span data-stu-id="933e1-109">The .NET Framework 4 introduced an asynchronous programming concept referred to as a [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) and ASP.NET MVC 4 supports [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span></span> <span data-ttu-id="933e1-110">任务由[tasks 命名空间中的](https://msdn.microsoft.com/library/system.threading.tasks.aspx)**任务**类型和相关类型来表示。</span><span class="sxs-lookup"><span data-stu-id="933e1-110">Tasks are represented by the **Task** type and related types in the [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) namespace.</span></span> <span data-ttu-id="933e1-111">.NET Framework 4.5 使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字构建这一异步支持，这些关键字使得处理[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)对象比以前的异步方法更复杂。</span><span class="sxs-lookup"><span data-stu-id="933e1-111">The .NET Framework 4.5 builds on this asynchronous support with the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords that make working with [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objects much less complex than previous asynchronous approaches.</span></span> <span data-ttu-id="933e1-112">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字是语法简写，指示代码段应以异步方式等待某个代码的其他部分。</span><span class="sxs-lookup"><span data-stu-id="933e1-112">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword is syntactical shorthand for indicating that a piece of code should asynchronously wait on some other piece of code.</span></span> <span data-ttu-id="933e1-113">[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字表示一个提示，可用于将方法标记为基于任务的异步方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-113">The [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword represents a hint that you can use to mark methods as task-based asynchronous methods.</span></span> <span data-ttu-id="933e1-114">**Await**、 **async**和**Task**对象的组合使你可以更轻松地在 .net 4.5 中编写异步代码。</span><span class="sxs-lookup"><span data-stu-id="933e1-114">The combination of **await**, **async**, and the **Task** object makes it much easier for you to write asynchronous code in .NET 4.5.</span></span> <span data-ttu-id="933e1-115">异步方法的新模型称为*基于任务的异步模式*（**点击**）。</span><span class="sxs-lookup"><span data-stu-id="933e1-115">The new model for asynchronous methods is called the *Task-based Asynchronous Pattern* (**TAP**).</span></span> <span data-ttu-id="933e1-116">本教程假定你熟悉使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字以及[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间的异步编程。</span><span class="sxs-lookup"><span data-stu-id="933e1-116">This tutorial assumes you have some familiarity with asynchronous programing using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace.</span></span>

<span data-ttu-id="933e1-117">有关使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字以及[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间的详细信息，请参阅以下参考。</span><span class="sxs-lookup"><span data-stu-id="933e1-117">For more information on the using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the following references.</span></span>

- [<span data-ttu-id="933e1-118">白皮书： .NET 中的异步</span><span class="sxs-lookup"><span data-stu-id="933e1-118">Whitepaper: Asynchrony in .NET</span></span>](https://go.microsoft.com/fwlink/?LinkId=204844)
- [<span data-ttu-id="933e1-119">Async/Await 常见问题</span><span class="sxs-lookup"><span data-stu-id="933e1-119">Async/Await FAQ</span></span>](https://blogs.msdn.com/b/pfxteam/archive/2012/04/12/10293335.aspx)
- [<span data-ttu-id="933e1-120">Visual Studio 异步编程</span><span class="sxs-lookup"><span data-stu-id="933e1-120">Visual Studio Asynchronous Programming</span></span>](https://msdn.microsoft.com/vstudio/gg316360)

## <a id="HowRequestsProcessedByTP"></a><span data-ttu-id="933e1-121">线程池如何处理请求</span><span class="sxs-lookup"><span data-stu-id="933e1-121">How Requests Are Processed by the Thread Pool</span></span>

<span data-ttu-id="933e1-122">在 web 服务器上，.NET Framework 维护用于处理 ASP.NET 请求的线程池。</span><span class="sxs-lookup"><span data-stu-id="933e1-122">On the web server, the .NET Framework maintains a pool of threads that are used to service ASP.NET requests.</span></span> <span data-ttu-id="933e1-123">当请求到达时，将调度池中的线程以处理该请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-123">When a request arrives, a thread from the pool is dispatched to process that request.</span></span> <span data-ttu-id="933e1-124">如果以同步方式处理请求，则处理请求的线程将在处理请求时处于繁忙状态，并且该线程无法处理其他请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-124">If the request is processed synchronously, the thread that processes the request is busy while the request is being processed, and that thread cannot service another request.</span></span>   
  
<span data-ttu-id="933e1-125">这可能不是问题，因为线程池的大小足以容纳多个繁忙线程。</span><span class="sxs-lookup"><span data-stu-id="933e1-125">This might not be a problem, because the thread pool can be made large enough to accommodate many busy threads.</span></span> <span data-ttu-id="933e1-126">但是，线程池中的线程数受到限制（.NET 4.5 的默认最大值是5000）。</span><span class="sxs-lookup"><span data-stu-id="933e1-126">However, the number of threads in the thread pool is limited (the default maximum for .NET 4.5 is 5,000).</span></span> <span data-ttu-id="933e1-127">在具有长时间运行的请求的高并发性的大型应用程序中，所有可用线程可能都处于繁忙状态。</span><span class="sxs-lookup"><span data-stu-id="933e1-127">In large applications with high concurrency of long-running requests, all available threads might be busy.</span></span> <span data-ttu-id="933e1-128">这种情况称为“线程不足”。</span><span class="sxs-lookup"><span data-stu-id="933e1-128">This condition is known as thread starvation.</span></span> <span data-ttu-id="933e1-129">达到此条件时，web 服务器会将请求排队。</span><span class="sxs-lookup"><span data-stu-id="933e1-129">When this condition is reached, the web server queues requests.</span></span> <span data-ttu-id="933e1-130">如果请求队列已满，则 web 服务器将拒绝 HTTP 503 状态（服务器太忙）的请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-130">If the request queue becomes full, the web server rejects requests with an HTTP 503 status (Server Too Busy).</span></span> <span data-ttu-id="933e1-131">CLR 线程池在新线程注入上具有限制。</span><span class="sxs-lookup"><span data-stu-id="933e1-131">The CLR thread pool has limitations on new thread injections.</span></span> <span data-ttu-id="933e1-132">如果并发为突发（也就是说，您的网站可以突然获得大量请求），并且所有可用的请求线程都忙于处理，因为具有高延迟的后端调用，所以，受限制的线程注入率会使应用程序的响应速度非常差。</span><span class="sxs-lookup"><span data-stu-id="933e1-132">If concurrency is bursty (that is, your web site can suddenly get a large number of requests) and all available request threads are busy because of backend calls with high latency, the limited thread injection rate can make your application respond very poorly.</span></span> <span data-ttu-id="933e1-133">此外，添加到线程池的每个新线程都具有开销（如 1 MB 的堆栈内存）。</span><span class="sxs-lookup"><span data-stu-id="933e1-133">Additionally, each new thread added to the thread pool has overhead (such as 1 MB of stack memory).</span></span> <span data-ttu-id="933e1-134">使用同步方法对高延迟调用进行服务的 web 应用程序，其中线程池增长到 .NET 4.5 默认值最多5，000个线程使用的内存大约为 5 GB异步方法和仅50线程。</span><span class="sxs-lookup"><span data-stu-id="933e1-134">A web application using synchronous methods to service high latency calls where the thread pool grows to the .NET 4.5 default maximum of 5, 000 threads would consume approximately 5 GB more memory than an application able the service the same requests using asynchronous methods and only 50 threads.</span></span> <span data-ttu-id="933e1-135">执行异步工作时，不一定要使用线程。</span><span class="sxs-lookup"><span data-stu-id="933e1-135">When you're doing asynchronous work, you're not always using a thread.</span></span> <span data-ttu-id="933e1-136">例如，当你发出异步 web 服务请求时，ASP.NET 将不会在**异步**方法调用与**await**之间使用任何线程。</span><span class="sxs-lookup"><span data-stu-id="933e1-136">For example, when you make an asynchronous web service request, ASP.NET will not be using any threads between the **async** method call and the **await**.</span></span> <span data-ttu-id="933e1-137">使用线程池来处理具有较高延迟的请求可能会导致内存占用量较大，并且服务器硬件利用率不佳。</span><span class="sxs-lookup"><span data-stu-id="933e1-137">Using the thread pool to service requests with high latency can lead to a large memory footprint and poor utilization of the server hardware.</span></span>

## <a name="processing-asynchronous-requests"></a><span data-ttu-id="933e1-138">处理异步请求</span><span class="sxs-lookup"><span data-stu-id="933e1-138">Processing Asynchronous Requests</span></span>

<span data-ttu-id="933e1-139">在启动时看到大量并发请求的 web 应用中，或具有突发负载（其中并发增长突然增加）时，使 web 服务调用异步会提高应用程序的响应能力。</span><span class="sxs-lookup"><span data-stu-id="933e1-139">In a web app that sees a large number of concurrent requests at start-up or has a bursty load (where concurrency increases suddenly), making web service calls asynchronous increases the responsiveness of the app.</span></span> <span data-ttu-id="933e1-140">异步请求与同步请求所需的处理时间相同。</span><span class="sxs-lookup"><span data-stu-id="933e1-140">An asynchronous request takes the same amount of time to process as a synchronous request.</span></span> <span data-ttu-id="933e1-141">如果请求发出需要两秒钟时间才能完成的 web 服务调用，则该请求将需要两秒钟，无论是同步执行还是异步执行。</span><span class="sxs-lookup"><span data-stu-id="933e1-141">If a request makes a web service call that requires two seconds to complete, the request takes two seconds whether it's performed synchronously or asynchronously.</span></span> <span data-ttu-id="933e1-142">但是，在异步调用期间，线程在等待第一个请求完成时不会被阻止响应其他请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-142">However during an asynchronous call, a thread isn't blocked from responding to other requests while it waits for the first request to complete.</span></span> <span data-ttu-id="933e1-143">因此，当有多个并发请求调用长时间运行的操作时，异步请求会阻止请求队列和线程池的增长。</span><span class="sxs-lookup"><span data-stu-id="933e1-143">Therefore, asynchronous requests prevent request queuing and thread pool growth when there are many concurrent requests that invoke long-running operations.</span></span>

## <a id="ChoosingSyncVasync"></a><span data-ttu-id="933e1-144">选择同步操作方法或异步操作方法</span><span class="sxs-lookup"><span data-stu-id="933e1-144">Choosing Synchronous or Asynchronous Action Methods</span></span>

<span data-ttu-id="933e1-145">本节列出了有关何时使用同步操作方法或异步操作方法的准则。</span><span class="sxs-lookup"><span data-stu-id="933e1-145">This section lists guidelines for when to use synchronous or asynchronous action methods.</span></span> <span data-ttu-id="933e1-146">这些只是指导原则;分别检查每个应用程序，以确定异步方法是否有助于提高性能。</span><span class="sxs-lookup"><span data-stu-id="933e1-146">These are just guidelines; examine each application individually to determine whether asynchronous methods help with performance.</span></span>

<span data-ttu-id="933e1-147">通常，在下列情况下使用同步方法：</span><span class="sxs-lookup"><span data-stu-id="933e1-147">In general, use synchronous methods for the following conditions:</span></span>

- <span data-ttu-id="933e1-148">操作很简单或运行时间很短。</span><span class="sxs-lookup"><span data-stu-id="933e1-148">The operations are simple or short-running.</span></span>
- <span data-ttu-id="933e1-149">简单性比效率更重要。</span><span class="sxs-lookup"><span data-stu-id="933e1-149">Simplicity is more important than efficiency.</span></span>
- <span data-ttu-id="933e1-150">此操作主要是 CPU 操作而不是包含大量的磁盘或网络开销的操作。</span><span class="sxs-lookup"><span data-stu-id="933e1-150">The operations are primarily CPU operations instead of operations that involve extensive disk or network overhead.</span></span> <span data-ttu-id="933e1-151">对 CPU 绑定操作使用异步操作方法未提供任何好处并且还导致更多的开销。</span><span class="sxs-lookup"><span data-stu-id="933e1-151">Using asynchronous action methods on CPU-bound operations provides no benefits and results in more overhead.</span></span>

<span data-ttu-id="933e1-152">通常，在下列情况下使用异步方法：</span><span class="sxs-lookup"><span data-stu-id="933e1-152">In general, use asynchronous methods for the following conditions:</span></span>

- <span data-ttu-id="933e1-153">正在调用可通过异步方法使用的服务，并且使用的是 .NET 4.5 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="933e1-153">You're calling services that can be consumed through asynchronous methods, and you're using .NET 4.5 or higher.</span></span>
- <span data-ttu-id="933e1-154">操作是网络绑定的或 I/O 绑定的而不是 CPU 绑定的。</span><span class="sxs-lookup"><span data-stu-id="933e1-154">The operations are network-bound or I/O-bound instead of CPU-bound.</span></span>
- <span data-ttu-id="933e1-155">并行性比代码的简单性更重要。</span><span class="sxs-lookup"><span data-stu-id="933e1-155">Parallelism is more important than simplicity of code.</span></span>
- <span data-ttu-id="933e1-156">您希望提供一种可让用户取消长时间运行的请求的机制。</span><span class="sxs-lookup"><span data-stu-id="933e1-156">You want to provide a mechanism that lets users cancel a long-running request.</span></span>
- <span data-ttu-id="933e1-157">切换线程的优点超出了上下文切换的成本。</span><span class="sxs-lookup"><span data-stu-id="933e1-157">When the benefit of switching threads outweighs the cost of the context switch.</span></span> <span data-ttu-id="933e1-158">通常，如果同步方法在 ASP.NET 请求线程上等待，而不执行任何操作，则应使方法异步。</span><span class="sxs-lookup"><span data-stu-id="933e1-158">In general, you should make a method asynchronous if the synchronous method waits on the ASP.NET request thread while doing no work.</span></span> <span data-ttu-id="933e1-159">通过将调用设为异步，ASP.NET 请求线程在等待 web 服务请求完成时，不会停止工作。</span><span class="sxs-lookup"><span data-stu-id="933e1-159">By making the call asynchronous, the ASP.NET request thread is not stalled doing no work while it waits for the web service request to complete.</span></span>
- <span data-ttu-id="933e1-160">测试表明阻塞操作是站点性能瓶颈，而 IIS 可以通过对这些阻止调用使用异步方法来提供更多请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-160">Testing shows that the blocking operations are a bottleneck in site performance and that IIS can service more requests by using asynchronous methods for these blocking calls.</span></span>

<span data-ttu-id="933e1-161">下载的示例演示如何有效地使用异步操作方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-161">The downloadable sample shows how to use asynchronous action methods effectively.</span></span> <span data-ttu-id="933e1-162">提供的示例旨在通过 .NET 4.5 在 ASP.NET MVC 4 中提供异步编程的简单演示。</span><span class="sxs-lookup"><span data-stu-id="933e1-162">The sample provided was designed to provide a simple demonstration of asynchronous programming in ASP.NET MVC 4 using .NET 4.5.</span></span> <span data-ttu-id="933e1-163">该示例不应作为 ASP.NET MVC 中异步编程的参考体系结构。</span><span class="sxs-lookup"><span data-stu-id="933e1-163">The sample is not intended to be a reference architecture for asynchronous programming in ASP.NET MVC.</span></span> <span data-ttu-id="933e1-164">该示例程序调用[ASP.NET Web API](../../../web-api/index.md)方法，这些方法进而调用[任务。延迟](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)以模拟长时间运行的 Web 服务调用。</span><span class="sxs-lookup"><span data-stu-id="933e1-164">The sample program calls [ASP.NET Web API](../../../web-api/index.md) methods which in turn call [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate long-running web service calls.</span></span> <span data-ttu-id="933e1-165">大多数生产应用程序将不会显示使用异步操作方法的明显优势。</span><span class="sxs-lookup"><span data-stu-id="933e1-165">Most production applications will not show such obvious benefits to using asynchronous action methods.</span></span>   
  
<span data-ttu-id="933e1-166">很少有应用程序要求所有的操作方法都是异步的。</span><span class="sxs-lookup"><span data-stu-id="933e1-166">Few applications require all action methods to be asynchronous.</span></span> <span data-ttu-id="933e1-167">通常，将少量的同步操作方法转换为异步方法就会显著增加所需的工作量。</span><span class="sxs-lookup"><span data-stu-id="933e1-167">Often, converting a few synchronous action methods to asynchronous methods provides the best efficiency increase for the amount of work required.</span></span>

## <a id="SampleApp"></a><span data-ttu-id="933e1-168">示例应用程序</span><span class="sxs-lookup"><span data-stu-id="933e1-168">The Sample Application</span></span>

<span data-ttu-id="933e1-169">你可以从[GitHub](https://github.com/)站点上的[https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET)下载示例应用程序。</span><span class="sxs-lookup"><span data-stu-id="933e1-169">You can download the sample application from [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET) on the [GitHub](https://github.com/) site.</span></span> <span data-ttu-id="933e1-170">存储库由三个项目组成：</span><span class="sxs-lookup"><span data-stu-id="933e1-170">The repository consists of three projects:</span></span>

- <span data-ttu-id="933e1-171">*Mvc4Async*： ASP.NET MVC 4 项目，其中包含本教程中使用的代码。</span><span class="sxs-lookup"><span data-stu-id="933e1-171">*Mvc4Async*: The ASP.NET MVC 4 project that contains the code used in this tutorial.</span></span> <span data-ttu-id="933e1-172">它使 Web API 调用**WebAPIpgw**服务。</span><span class="sxs-lookup"><span data-stu-id="933e1-172">It makes Web API calls to the **WebAPIpgw** service.</span></span>
- <span data-ttu-id="933e1-173">*WebAPIpgw*：实现 `Products, Gizmos and Widgets` 控制器的 ASP.NET MVC 4 Web API 项目。</span><span class="sxs-lookup"><span data-stu-id="933e1-173">*WebAPIpgw*: The ASP.NET MVC 4 Web API project that implements the `Products, Gizmos and Widgets` controllers.</span></span> <span data-ttu-id="933e1-174">它为*WebAppAsync*项目和*Mvc4Async*项目提供数据。</span><span class="sxs-lookup"><span data-stu-id="933e1-174">It provides the data for the *WebAppAsync* project and the *Mvc4Async* project.</span></span>
- <span data-ttu-id="933e1-175">*WebAppAsync*：在其他教程中使用的 ASP.NET Web 窗体项目。</span><span class="sxs-lookup"><span data-stu-id="933e1-175">*WebAppAsync*: The ASP.NET Web Forms project used in another tutorial.</span></span>

## <a id="GizmosSynch"></a><span data-ttu-id="933e1-176">Gizmos 同步操作方法</span><span class="sxs-lookup"><span data-stu-id="933e1-176">The Gizmos Synchronous Action Method</span></span>

 <span data-ttu-id="933e1-177">下面的代码演示了用于显示 gizmos 列表的 `Gizmos` 同步操作方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-177">The following code shows the `Gizmos` synchronous action method that is used to display a list of gizmos.</span></span> <span data-ttu-id="933e1-178">（对于本文，别出心裁是虚构的机械设备。）</span><span class="sxs-lookup"><span data-stu-id="933e1-178">(For this article, a gizmo is a fictional mechanical device.)</span></span> 

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample1.cs)]

<span data-ttu-id="933e1-179">下面的代码演示别出心裁服务的 `GetGizmos` 方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-179">The following code shows the `GetGizmos` method of the gizmo service.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample2.cs)]

<span data-ttu-id="933e1-180">`GizmoService GetGizmos` 方法将 URI 传递到 ASP.NET Web API HTTP 服务，该服务返回 gizmos 数据列表。</span><span class="sxs-lookup"><span data-stu-id="933e1-180">The `GizmoService GetGizmos` method passes a URI to an ASP.NET Web API HTTP service which returns a list of gizmos data.</span></span> <span data-ttu-id="933e1-181">*WebAPIpgw*项目包含 `gizmos, widget` 和 `product` 控制器的 Web API 实现。</span><span class="sxs-lookup"><span data-stu-id="933e1-181">The *WebAPIpgw* project contains the implementation of the Web API `gizmos, widget` and `product` controllers.</span></span>  
<span data-ttu-id="933e1-182">下图显示了示例项目中的 gizmos 视图。</span><span class="sxs-lookup"><span data-stu-id="933e1-182">The following image shows the gizmos view from the sample project.</span></span>

![Gizmos](using-asynchronous-methods-in-aspnet-mvc-4/_static/image1.png)

## <a id="CreatingAsynchGizmos"></a><span data-ttu-id="933e1-184">创建异步 Gizmos 操作方法</span><span class="sxs-lookup"><span data-stu-id="933e1-184">Creating an Asynchronous Gizmos Action Method</span></span>

<span data-ttu-id="933e1-185">该示例使用新的[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)和[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字（在 .Net 4.5 和 Visual Studio 2012 中提供），让编译器负责维护异步编程所需的复杂转换。</span><span class="sxs-lookup"><span data-stu-id="933e1-185">The sample uses the new [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) and [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keywords (available in .NET 4.5 and Visual Studio 2012) to let the compiler be responsible for maintaining the complicated transformations necessary for asynchronous programming.</span></span> <span data-ttu-id="933e1-186">编译器使你可以使用C#的同步控制流构造编写代码，并且编译器会自动应用使用回调所需的转换，以避免阻塞线程。</span><span class="sxs-lookup"><span data-stu-id="933e1-186">The compiler lets you write code using the C#'s synchronous control flow constructs and the compiler automatically applies the transformations necessary to use callbacks in order to avoid blocking threads.</span></span>

<span data-ttu-id="933e1-187">下面的代码演示 `Gizmos` 同步方法和 `GizmosAsync` 异步方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-187">The following code shows the `Gizmos` synchronous method and the `GizmosAsync` asynchronous method.</span></span> <span data-ttu-id="933e1-188">如果你的浏览器支持[HTML 5 `<mark>` 元素](http://www.w3.org/wiki/HTML/Elements/mark)，你会在 `GizmosAsync` 中看到黄色突出显示的更改。</span><span class="sxs-lookup"><span data-stu-id="933e1-188">If your browser supports the [HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), you'll see the changes in `GizmosAsync` in yellow highlight.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample3.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample4.cs?highlight=1,3,5)]

 <span data-ttu-id="933e1-189">应用了以下更改，以允许 `GizmosAsync` 是异步的。</span><span class="sxs-lookup"><span data-stu-id="933e1-189">The following changes were applied to allow the `GizmosAsync` to be asynchronous.</span></span>

- <span data-ttu-id="933e1-190">方法标记有[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字，该关键字告诉编译器为部分主体生成回调并自动创建返回的 `Task<ActionResult>`。</span><span class="sxs-lookup"><span data-stu-id="933e1-190">The method is marked with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, which tells the compiler to generate callbacks for parts of the body and to automatically create a `Task<ActionResult>` that is returned.</span></span>
- <span data-ttu-id="933e1-191">&quot;Async&quot; 追加到方法名称。</span><span class="sxs-lookup"><span data-stu-id="933e1-191">&quot;Async&quot; was appended to the method name.</span></span> <span data-ttu-id="933e1-192">不需要追加 "Async"，但这是编写异步方法时的约定。</span><span class="sxs-lookup"><span data-stu-id="933e1-192">Appending "Async" is not required but is the convention when writing asynchronous methods.</span></span>
- <span data-ttu-id="933e1-193">返回类型已从 `ActionResult` 更改为 `Task<ActionResult>`。</span><span class="sxs-lookup"><span data-stu-id="933e1-193">The return type was changed from `ActionResult` to `Task<ActionResult>`.</span></span> <span data-ttu-id="933e1-194">`Task<ActionResult>` 的返回类型表示正在进行的工作，并为方法的调用方提供一个句柄，用于等待异步操作完成。</span><span class="sxs-lookup"><span data-stu-id="933e1-194">The return type of `Task<ActionResult>` represents ongoing work and provides callers of the method with a handle through which to wait for the asynchronous operation's completion.</span></span> <span data-ttu-id="933e1-195">在这种情况下，调用方为 web 服务。</span><span class="sxs-lookup"><span data-stu-id="933e1-195">In this case, the caller is the web service.</span></span> <span data-ttu-id="933e1-196">`Task<ActionResult>` 表示 `ActionResult.` 的结果中的正在进行的工作</span><span class="sxs-lookup"><span data-stu-id="933e1-196">`Task<ActionResult>` represents ongoing work with a result of `ActionResult.`</span></span>
- <span data-ttu-id="933e1-197">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字已应用于 web 服务调用。</span><span class="sxs-lookup"><span data-stu-id="933e1-197">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the web service call.</span></span>
- <span data-ttu-id="933e1-198">调用了异步 web 服务 API （`GetGizmosAsync`）。</span><span class="sxs-lookup"><span data-stu-id="933e1-198">The asynchronous web service API was called (`GetGizmosAsync`).</span></span>

<span data-ttu-id="933e1-199">在 `GetGizmosAsync` 方法体内部，调用了另一个异步方法 `GetGizmosAsync`。</span><span class="sxs-lookup"><span data-stu-id="933e1-199">Inside of the `GetGizmosAsync` method body another asynchronous method, `GetGizmosAsync` is called.</span></span> <span data-ttu-id="933e1-200">`GetGizmosAsync` 立即返回一个将在数据可用时最终完成的 `Task<List<Gizmo>>`。</span><span class="sxs-lookup"><span data-stu-id="933e1-200">`GetGizmosAsync` immediately returns a `Task<List<Gizmo>>` that will eventually complete when the data is available.</span></span> <span data-ttu-id="933e1-201">因为在有别出心裁数据之前不需要执行任何其他操作，因此代码会等待任务（使用**await**关键字）。</span><span class="sxs-lookup"><span data-stu-id="933e1-201">Because you don't want to do anything else until you have the gizmo data, the code awaits the task (using the **await** keyword).</span></span> <span data-ttu-id="933e1-202">只能在使用**async**关键字批注的方法中使用**await**关键字。</span><span class="sxs-lookup"><span data-stu-id="933e1-202">You can use the **await** keyword only in methods annotated with the **async** keyword.</span></span>

<span data-ttu-id="933e1-203">**等待**关键字不会阻止线程，直到任务完成。</span><span class="sxs-lookup"><span data-stu-id="933e1-203">The **await** keyword does not block the thread until the task is complete.</span></span> <span data-ttu-id="933e1-204">它将方法的其余部分注册为任务的回调，并立即返回。</span><span class="sxs-lookup"><span data-stu-id="933e1-204">It signs up the rest of the method as a callback on the task, and immediately returns.</span></span> <span data-ttu-id="933e1-205">当等待的任务最终完成时，它将调用该回调，并因此在其中断时继续执行方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-205">When the awaited task eventually completes, it will invoke that callback and thus resume the execution of the method right where it left off.</span></span> <span data-ttu-id="933e1-206">有关使用[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[Async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字以及[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间的详细信息，请参阅[async 引用](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async)。</span><span class="sxs-lookup"><span data-stu-id="933e1-206">For more information on using the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the [async references](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span></span>

<span data-ttu-id="933e1-207">以下代码显示了 `GetGizmos` 和 `GetGizmosAsync` 方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-207">The following code shows the `GetGizmos` and `GetGizmosAsync` methods.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample5.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample6.cs?highlight=1,4-8)]

 <span data-ttu-id="933e1-208">异步更改与上述**GizmosAsync**的更改类似。</span><span class="sxs-lookup"><span data-stu-id="933e1-208">The asynchronous changes are similar to those made to the **GizmosAsync** above.</span></span> 

- <span data-ttu-id="933e1-209">方法签名通过[async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字进行批注，返回类型已更改为 `Task<List<Gizmo>>`，而*async*追加到方法名称。</span><span class="sxs-lookup"><span data-stu-id="933e1-209">The method signature was annotated with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, the return type was changed to `Task<List<Gizmo>>`, and *Async* was appended to the method name.</span></span>
- <span data-ttu-id="933e1-210">使用异步[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)类而不是[WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx)类。</span><span class="sxs-lookup"><span data-stu-id="933e1-210">The asynchronous [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) class is used instead of the [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) class.</span></span>
- <span data-ttu-id="933e1-211">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字已应用于[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)异步方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-211">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronous methods.</span></span>

<span data-ttu-id="933e1-212">下图显示了异步别出心裁视图。</span><span class="sxs-lookup"><span data-stu-id="933e1-212">The following image shows the asynchronous gizmo view.</span></span>

![async](using-asynchronous-methods-in-aspnet-mvc-4/_static/image2.png)

<span data-ttu-id="933e1-214">Gizmos 数据的浏览器表示形式与同步调用创建的视图相同。</span><span class="sxs-lookup"><span data-stu-id="933e1-214">The browsers presentation of the gizmos data is identical to the view created by the synchronous call.</span></span> <span data-ttu-id="933e1-215">唯一的区别是，在负载较重的情况下，异步版本的性能可能更高。</span><span class="sxs-lookup"><span data-stu-id="933e1-215">The only difference is the asynchronous version may be more performant under heavy loads.</span></span>

## <a id="Parallel"></a><span data-ttu-id="933e1-216">并行执行多个操作</span><span class="sxs-lookup"><span data-stu-id="933e1-216">Performing Multiple Operations in Parallel</span></span>

<span data-ttu-id="933e1-217">当操作必须执行多个独立的操作时，异步操作方法与同步方法相比具有明显的优势。</span><span class="sxs-lookup"><span data-stu-id="933e1-217">Asynchronous action methods have a significant advantage over synchronous methods when an action must perform several independent operations.</span></span> <span data-ttu-id="933e1-218">在提供的示例中，同步方法 `PWG`（适用于产品、小组件和 Gizmos）显示三个 web 服务调用的结果，以获取产品、小组件和 Gizmos 的列表。</span><span class="sxs-lookup"><span data-stu-id="933e1-218">In the sample provided, the synchronous method `PWG`(for Products, Widgets and Gizmos) displays the results of three web service calls to get a list of products, widgets, and gizmos.</span></span> <span data-ttu-id="933e1-219">提供这些服务的[ASP.NET Web API](../../../web-api/index.md)项目使用[任务延迟](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)来模拟延迟或慢速网络调用。</span><span class="sxs-lookup"><span data-stu-id="933e1-219">The [ASP.NET Web API](../../../web-api/index.md) project that provides these services uses [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate latency or slow network calls.</span></span> <span data-ttu-id="933e1-220">当延迟设置为500毫秒时，异步 `PWGasync` 方法将需要一小段500以上的时间才能完成，同时同步 `PWG` 版本需要超过1500毫秒。</span><span class="sxs-lookup"><span data-stu-id="933e1-220">When the delay is set to 500 milliseconds, the asynchronous `PWGasync` method takes a little over 500 milliseconds to complete while the synchronous `PWG` version takes over 1,500 milliseconds.</span></span> <span data-ttu-id="933e1-221">下面的代码演示了同步 `PWG` 方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-221">The synchronous `PWG` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample7.cs)]

<span data-ttu-id="933e1-222">下面的代码演示了异步 `PWGasync` 方法。</span><span class="sxs-lookup"><span data-stu-id="933e1-222">The asynchronous `PWGasync` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample8.cs?highlight=1,3,12)]

<span data-ttu-id="933e1-223">下图显示了从**PWGasync**方法返回的视图。</span><span class="sxs-lookup"><span data-stu-id="933e1-223">The following image shows the view returned from the **PWGasync** method.</span></span>

![pwgAsync](using-asynchronous-methods-in-aspnet-mvc-4/_static/image3.png)

## <a id="CancelToken"></a><span data-ttu-id="933e1-225">使用取消标记</span><span class="sxs-lookup"><span data-stu-id="933e1-225">Using a Cancellation Token</span></span>

<span data-ttu-id="933e1-226">返回 `Task<ActionResult>`的异步操作方法可取消，即当使用[AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx)特性提供一个参数时，它们将采用[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)参数。</span><span class="sxs-lookup"><span data-stu-id="933e1-226">Asynchronous action methods returning `Task<ActionResult>`are cancelable, that is they take a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter when one is provided with the [AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) attribute.</span></span> <span data-ttu-id="933e1-227">下面的代码演示 `GizmosCancelAsync` 方法，超时为150毫秒。</span><span class="sxs-lookup"><span data-stu-id="933e1-227">The following code shows the `GizmosCancelAsync` method with a timeout of 150 milliseconds.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample9.cs?highlight=1-3,5,10)]

<span data-ttu-id="933e1-228">下面的代码演示 GetGizmosAsync 重载，它采用[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)参数。</span><span class="sxs-lookup"><span data-stu-id="933e1-228">The following code shows the GetGizmosAsync overload, which takes a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample10.cs)]

<span data-ttu-id="933e1-229">在提供的示例应用程序中，选择 "*取消标记" 演示*链接将调用 `GizmosCancelAsync` 方法，并演示异步调用的取消。</span><span class="sxs-lookup"><span data-stu-id="933e1-229">In the sample application provided, selecting the *Cancellation Token Demo* link calls the `GizmosCancelAsync` method and demonstrates the cancellation of the asynchronous call.</span></span>

## <a id="ServerConfig"></a><span data-ttu-id="933e1-230">高并发/高延迟 Web 服务调用的服务器配置</span><span class="sxs-lookup"><span data-stu-id="933e1-230">Server Configuration for High Concurrency/High Latency Web Service Calls</span></span>

<span data-ttu-id="933e1-231">若要实现异步 web 应用程序的优势，可能需要对默认服务器配置进行一些更改。</span><span class="sxs-lookup"><span data-stu-id="933e1-231">To realize the benefits of an asynchronous web application, you might need to make some changes to the default server configuration.</span></span> <span data-ttu-id="933e1-232">配置和压力测试异步 web 应用程序时，请记住以下几点。</span><span class="sxs-lookup"><span data-stu-id="933e1-232">Keep the following in mind when configuring and stress testing your asynchronous web application.</span></span>

- <span data-ttu-id="933e1-233">Windows 7、Windows Vista 和所有 Windows 客户端操作系统最多可以有10个并发请求。</span><span class="sxs-lookup"><span data-stu-id="933e1-233">Windows 7, Windows Vista and all Windows client operating systems have a maximum of 10 concurrent requests.</span></span> <span data-ttu-id="933e1-234">你将需要 Windows Server 操作系统，以查看高负载下的异步方法的优点。</span><span class="sxs-lookup"><span data-stu-id="933e1-234">You'll need a Windows Server operating system to see the benefits of asynchronous methods under high load.</span></span>
- <span data-ttu-id="933e1-235">在提升的命令提示符下向 IIS 注册 .NET 4.5：</span><span class="sxs-lookup"><span data-stu-id="933e1-235">Register .NET 4.5 with IIS from an elevated command prompt:</span></span>  
  <span data-ttu-id="933e1-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis-i</span><span class="sxs-lookup"><span data-stu-id="933e1-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span></span>  
  <span data-ttu-id="933e1-237">请参阅[ASP.NET IIS 注册工具（Aspnet\_regiis）](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span><span class="sxs-lookup"><span data-stu-id="933e1-237">See    [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span></span>
- <span data-ttu-id="933e1-238">可能需要将[HTTP .sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue 限制从默认值1000提高到5000。</span><span class="sxs-lookup"><span data-stu-id="933e1-238">You might need to increase the [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue limit from the default value of 1,000 to 5,000.</span></span> <span data-ttu-id="933e1-239">如果设置过低，可能会看到[http： .sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture)拒绝请求，并显示 http 503 状态。</span><span class="sxs-lookup"><span data-stu-id="933e1-239">If the setting is too low, you may see [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) reject requests with a HTTP 503 status.</span></span> <span data-ttu-id="933e1-240">更改 http.sys 队列限制：</span><span class="sxs-lookup"><span data-stu-id="933e1-240">To change the HTTP.sys queue limit:</span></span>

    - <span data-ttu-id="933e1-241">打开 IIS 管理器并导航到 "应用程序池" 窗格。</span><span class="sxs-lookup"><span data-stu-id="933e1-241">Open IIS manager and navigate to the Application Pools pane.</span></span>
    - <span data-ttu-id="933e1-242">右键单击目标应用程序池，然后选择 "**高级设置**"。</span><span class="sxs-lookup"><span data-stu-id="933e1-242">Right click on the target application pool and select **Advanced Settings**.</span></span>  
        <span data-ttu-id="933e1-243">![高级](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="933e1-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span></span>
    - <span data-ttu-id="933e1-244">在 "**高级设置**" 对话框中，将*队列长度*从1000更改为5000。</span><span class="sxs-lookup"><span data-stu-id="933e1-244">In the **Advanced Settings** dialog box, change *Queue Length* from 1,000 to 5,000.</span></span>  
        <span data-ttu-id="933e1-245">![队列长度](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="933e1-245">![Queue length](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span></span>  
  
  <span data-ttu-id="933e1-246">请注意，在上面的图像中，.NET framework 作为 "v 4.0" 列出，即使应用程序池使用的是 .NET 4.5 也是如此。</span><span class="sxs-lookup"><span data-stu-id="933e1-246">Note in the images above, the .NET framework is listed as v4.0, even though the application pool is using .NET 4.5.</span></span> <span data-ttu-id="933e1-247">要了解这种差异，请参阅以下内容：</span><span class="sxs-lookup"><span data-stu-id="933e1-247">To understand this discrepancy, see the following:</span></span>

    - [<span data-ttu-id="933e1-248">.NET 版本控制和多目标 .NET 4.5 是 .NET 4.0 的就地升级</span><span class="sxs-lookup"><span data-stu-id="933e1-248">.NET Versioning and Multi-Targeting - .NET 4.5 is an in-place upgrade to .NET 4.0</span></span>](http://www.hanselman.com/blog/NETVersioningAndMultiTargetingNET45IsAnInplaceUpgradeToNET40.aspx)
    - [<span data-ttu-id="933e1-249">如何将 IIS 应用程序或 AppPool 设置为使用 ASP.NET 3.5，而不是2。0</span><span class="sxs-lookup"><span data-stu-id="933e1-249">How to set an IIS Application or AppPool to use ASP.NET 3.5 rather than 2.0</span></span>](http://www.hanselman.com/blog/HowToSetAnIISApplicationOrAppPoolToUseASPNET35RatherThan20.aspx)
    - <span data-ttu-id="933e1-250">[.NET Framework 版本和依赖关系](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="933e1-250">[.NET Framework Versions and Dependencies](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span></span>
- <span data-ttu-id="933e1-251">如果你的应用程序使用 web 服务或 System.NET 通过 HTTP 与后端通信，则你可能需要增加[connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx)元素。</span><span class="sxs-lookup"><span data-stu-id="933e1-251">If your application is using web services or System.NET to communicate with a backend over HTTP you may need to increase the [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) element.</span></span> <span data-ttu-id="933e1-252">对于 ASP.NET 应用程序，这被自动配置功能限制为 Cpu 数量的12倍。</span><span class="sxs-lookup"><span data-stu-id="933e1-252">For ASP.NET applications, this is limited by the autoConfig feature to 12 times the number of CPUs.</span></span> <span data-ttu-id="933e1-253">这意味着，在四处理器上，最多可以有12个 \* 4 = 48 并发连接到 IP 终结点。</span><span class="sxs-lookup"><span data-stu-id="933e1-253">That means that on a quad-proc, you can have at most 12 \* 4 = 48 concurrent connections to an IP end point.</span></span> <span data-ttu-id="933e1-254">由于此方法已绑定到自动[配置](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx)，因此在 ASP.NET 应用程序中增加 `maxconnection` 的最简单方法是在*global.asax*文件的 from `Application_Start` 方法中以编程方式设置[ServicePointManager servicepointmanager.defaultconnectionlimit。](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="933e1-254">Because this is tied to [autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), the easiest way to increase `maxconnection` in an ASP.NET application is to set [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programmatically in the from `Application_Start` method in the *global.asax* file.</span></span> <span data-ttu-id="933e1-255">有关示例，请参阅示例下载。</span><span class="sxs-lookup"><span data-stu-id="933e1-255">See the sample download for an example.</span></span>
- <span data-ttu-id="933e1-256">在 .NET 4.5 中， [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx)的默认值为5000。</span><span class="sxs-lookup"><span data-stu-id="933e1-256">In .NET 4.5, the default of 5000 for [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) should be fine.</span></span>
