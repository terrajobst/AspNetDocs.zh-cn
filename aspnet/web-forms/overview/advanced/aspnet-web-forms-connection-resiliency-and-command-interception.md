---
uid: web-forms/overview/advanced/aspnet-web-forms-connection-resiliency-and-command-interception
title: ASP.NET Web Forms 连接复原和命令侦听 |Microsoft Docs
author: Erikre
description: 本教程介绍如何修改示例应用程序，以支持连接复原和命令侦听。
ms.author: riande
ms.date: 03/31/2014
ms.assetid: 6d497001-fa80-4765-b4cc-181fe90b894e
msc.legacyurl: /web-forms/overview/advanced/aspnet-web-forms-connection-resiliency-and-command-interception
msc.type: authoredcontent
ms.openlocfilehash: 95f0b5635c12d5ef88622e5766c1278c6570dd4d
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/06/2020
ms.locfileid: "78484232"
---
# <a name="aspnet-web-forms-connection-resiliency-and-command-interception"></a><span data-ttu-id="43084-103">ASP.NET Web 窗体连接复原和命令截获</span><span class="sxs-lookup"><span data-stu-id="43084-103">ASP.NET Web Forms Connection Resiliency and Command Interception</span></span>

<span data-ttu-id="43084-104">作者： [Erik Reitan](https://github.com/Erikre)</span><span class="sxs-lookup"><span data-stu-id="43084-104">by [Erik Reitan](https://github.com/Erikre)</span></span>

<span data-ttu-id="43084-105">在本教程中，您将修改 Wingtip 玩具示例应用程序，以支持连接复原和命令拦截。</span><span class="sxs-lookup"><span data-stu-id="43084-105">In this tutorial, you will modify the Wingtip Toys sample application to support connection resiliency and command interception.</span></span> <span data-ttu-id="43084-106">启用连接复原后，当发生云环境的典型暂时性错误时，Wingtip 玩具示例应用程序将自动重试数据调用。</span><span class="sxs-lookup"><span data-stu-id="43084-106">By enabling connection resiliency, the Wingtip Toys sample application will automatically retry data calls when transient errors typical of a cloud environment occur.</span></span> <span data-ttu-id="43084-107">此外，通过实现命令拦截，Wingtip 玩具示例应用程序将捕获发送到数据库的所有 SQL 查询，以便记录或更改这些查询。</span><span class="sxs-lookup"><span data-stu-id="43084-107">Also, by implementing command interception, the Wingtip Toys sample application will catch all SQL queries sent to the database in order to log or change them.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="43084-108">此 Web 窗体教程基于 Tom Dykstra 的以下 MVC 教程：</span><span class="sxs-lookup"><span data-stu-id="43084-108">This Web Forms tutorial was based on Tom Dykstra's following MVC tutorial:</span></span>  
> [<span data-ttu-id="43084-109">ASP.NET MVC 应用程序中的实体框架的连接复原和命令截获</span><span class="sxs-lookup"><span data-stu-id="43084-109">Connection Resiliency and Command Interception with the Entity Framework in an ASP.NET MVC Application</span></span>](../../../mvc/overview/getting-started/getting-started-with-ef-using-mvc/connection-resiliency-and-command-interception-with-the-entity-framework-in-an-asp-net-mvc-application.md)

## <a name="what-youll-learn"></a><span data-ttu-id="43084-110">学习内容：</span><span class="sxs-lookup"><span data-stu-id="43084-110">What you'll learn:</span></span>

- <span data-ttu-id="43084-111">如何提供连接复原能力。</span><span class="sxs-lookup"><span data-stu-id="43084-111">How to provide connection resiliency.</span></span>
- <span data-ttu-id="43084-112">如何实现命令截取。</span><span class="sxs-lookup"><span data-stu-id="43084-112">How to implement command interception.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="43084-113">系统必备</span><span class="sxs-lookup"><span data-stu-id="43084-113">Prerequisites</span></span>

<span data-ttu-id="43084-114">在开始之前，请确保计算机上已安装了以下软件：</span><span class="sxs-lookup"><span data-stu-id="43084-114">Before you start, make sure that you have the following software installed on your computer:</span></span>

- <span data-ttu-id="43084-115">[Microsoft Visual Studio 2013](https://www.microsoft.com/visualstudio/11/downloads#vs)或[Microsoft Visual Studio Express 2013 for Web](https://www.microsoft.com/visualstudio/11/downloads#express-web)。</span><span class="sxs-lookup"><span data-stu-id="43084-115">[Microsoft Visual Studio 2013](https://www.microsoft.com/visualstudio/11/downloads#vs) or [Microsoft Visual Studio Express 2013 for Web](https://www.microsoft.com/visualstudio/11/downloads#express-web).</span></span> <span data-ttu-id="43084-116">将自动安装 .NET Framework。</span><span class="sxs-lookup"><span data-stu-id="43084-116">The .NET Framework is installed automatically.</span></span>
- <span data-ttu-id="43084-117">Wingtip 玩具示例项目，以便可以在 Wingtip 玩具项目中实现本教程中所述的功能。</span><span class="sxs-lookup"><span data-stu-id="43084-117">The Wingtip Toys sample project, so that you can implement the functionality mentioned in this tutorial within the Wingtip Toys project.</span></span> <span data-ttu-id="43084-118">以下链接提供了下载详细信息：</span><span class="sxs-lookup"><span data-stu-id="43084-118">The following link provides download details:</span></span>

    - <span data-ttu-id="43084-119">[入门 4.5.1 Web 窗体-Wingtip 玩具](https://go.microsoft.com/fwlink/?LinkID=389434&amp;clcid=0x409)（C#）</span><span class="sxs-lookup"><span data-stu-id="43084-119">[Getting Started with ASP.NET 4.5.1 Web Forms - Wingtip Toys](https://go.microsoft.com/fwlink/?LinkID=389434&amp;clcid=0x409) (C#)</span></span>
- <span data-ttu-id="43084-120">在完成本教程之前，请考虑查看相关的教程系列，[并入门 ASP.NET 4.5 Web Forms 和 Visual Studio 2013](../getting-started/getting-started-with-aspnet-45-web-forms/introduction-and-overview.md)。</span><span class="sxs-lookup"><span data-stu-id="43084-120">Prior to completing this tutorial, consider reviewing the related tutorial series, [Getting Started with ASP.NET 4.5 Web Forms and Visual Studio 2013](../getting-started/getting-started-with-aspnet-45-web-forms/introduction-and-overview.md).</span></span> <span data-ttu-id="43084-121">本教程系列将帮助你熟悉**WingtipToys**项目和代码。</span><span class="sxs-lookup"><span data-stu-id="43084-121">The tutorial series will help you become familiar with the **WingtipToys** project and code.</span></span>

## <a name="connection-resiliency"></a><span data-ttu-id="43084-122">连接复原</span><span class="sxs-lookup"><span data-stu-id="43084-122">Connection resiliency</span></span>

<span data-ttu-id="43084-123">考虑将应用程序部署到 Windows Azure 时，需要考虑的一个选项是将数据库部署到**microsoft** **azure SQL 数据库**（一个云数据库服务）。</span><span class="sxs-lookup"><span data-stu-id="43084-123">When you consider deploying an application to Windows Azure, one option to consider is deploying the database to **Windows** **Azure SQL Database**, a cloud database service.</span></span> <span data-ttu-id="43084-124">当你连接到云数据库服务时，如果连接到同一数据中心的 web 服务器和数据库服务器直接连接到云数据库服务，则暂时性连接错误通常会更频繁。</span><span class="sxs-lookup"><span data-stu-id="43084-124">Transient connection errors are typically more frequent when you connect to a cloud database service than when your web server and your database server are directly connected together in the same data center.</span></span> <span data-ttu-id="43084-125">即使云 web 服务器和云数据库服务托管在同一数据中心，它们之间的网络连接也可能存在问题，例如负载均衡器。</span><span class="sxs-lookup"><span data-stu-id="43084-125">Even if a cloud web server and a cloud database service are hosted in the same data center, there are more network connections between them that can have problems, such as load balancers.</span></span>

<span data-ttu-id="43084-126">此外，云服务通常由其他用户共享，这意味着其响应能力可能会受到影响。</span><span class="sxs-lookup"><span data-stu-id="43084-126">Also a cloud service is typically shared by other users, which means its responsiveness can be affected by them.</span></span> <span data-ttu-id="43084-127">您对数据库的访问可能会受到限制。</span><span class="sxs-lookup"><span data-stu-id="43084-127">And your access to the database might be subject to throttling.</span></span> <span data-ttu-id="43084-128">限制意味着，当你尝试比你的*服务级别协议*（SLA）中的允许更频繁地访问它时，数据库服务将引发异常。</span><span class="sxs-lookup"><span data-stu-id="43084-128">Throttling means the database service throws exceptions when you try to access it more frequently than is allowed in your *Service Level Agreement* (SLA).</span></span>

<span data-ttu-id="43084-129">访问云服务时出现的许多或大多数连接问题都是暂时性的，也就是说，它们会在短时间内解决。</span><span class="sxs-lookup"><span data-stu-id="43084-129">Many or most connection problems that occur when you're accessing a cloud service are transient, that is, they resolve themselves in a short period of time.</span></span> <span data-ttu-id="43084-130">因此，当你尝试数据库操作并获取通常是暂时性的错误类型时，你可以在短时间等待后再次尝试该操作，并且该操作可能成功。</span><span class="sxs-lookup"><span data-stu-id="43084-130">So when you try a database operation and get a type of error that is typically transient, you could try the operation again after a short wait, and the operation might be successful.</span></span> <span data-ttu-id="43084-131">如果你通过自动重试来处理暂时性错误，则你可以为用户提供更好的体验，使他们对客户不可见。</span><span class="sxs-lookup"><span data-stu-id="43084-131">You can provide a much better experience for your users if you handle transient errors by automatically trying again, making most of them invisible to the customer.</span></span> <span data-ttu-id="43084-132">实体框架6中的连接复原功能自动执行重试失败的 SQL 查询这一过程。</span><span class="sxs-lookup"><span data-stu-id="43084-132">The connection resiliency feature in Entity Framework 6 automates that process of retrying failed SQL queries.</span></span>

<span data-ttu-id="43084-133">必须为特定数据库服务正确配置连接复原功能：</span><span class="sxs-lookup"><span data-stu-id="43084-133">The connection resiliency feature must be configured appropriately for a particular database service:</span></span>

1. <span data-ttu-id="43084-134">它必须知道哪些异常可能是暂时性的。</span><span class="sxs-lookup"><span data-stu-id="43084-134">It has to know which exceptions are likely to be transient.</span></span> <span data-ttu-id="43084-135">您希望重试由于网络连接暂时丢失而导致的错误，例如，不是由程序错误导致的错误。</span><span class="sxs-lookup"><span data-stu-id="43084-135">You want to retry errors caused by a temporary loss in network connectivity, not errors caused by program bugs, for example.</span></span>
2. <span data-ttu-id="43084-136">它必须等待一段适当的时间，重试失败的操作。</span><span class="sxs-lookup"><span data-stu-id="43084-136">It has to wait an appropriate amount of time between retries of a failed operation.</span></span> <span data-ttu-id="43084-137">在批处理过程中，你可以等待多长时间才能进行批处理，而不能在用户等待响应的联机网页之间等待。</span><span class="sxs-lookup"><span data-stu-id="43084-137">You can wait longer between retries for a batch process than you can for an online web page where a user is waiting for a response.</span></span>
3. <span data-ttu-id="43084-138">它必须在放弃之前重试相应的次数。</span><span class="sxs-lookup"><span data-stu-id="43084-138">It has to retry an appropriate number of times before it gives up.</span></span> <span data-ttu-id="43084-139">你可能需要在联机应用程序的批处理过程中重试更多次。</span><span class="sxs-lookup"><span data-stu-id="43084-139">You might want to retry more times in a batch process that you would in an online application.</span></span>

<span data-ttu-id="43084-140">你可以为实体框架提供程序支持的任何数据库环境手动配置这些设置。</span><span class="sxs-lookup"><span data-stu-id="43084-140">You can configure these settings manually for any database environment supported by an Entity Framework provider.</span></span>

<span data-ttu-id="43084-141">若要启用连接复原，只需在程序集中创建一个派生自 `DbConfiguration` 类的类，并在该类中设置 SQL 数据库执行策略，该策略在实体框架中是重试策略的另一种术语。</span><span class="sxs-lookup"><span data-stu-id="43084-141">All you have to do to enable connection resiliency is create a class in your assembly that derives from the `DbConfiguration` class, and in that class set the SQL Database execution strategy, which in Entity Framework is another term for retry policy.</span></span>

### <a name="implementing-connection-resiliency"></a><span data-ttu-id="43084-142">实现连接复原</span><span class="sxs-lookup"><span data-stu-id="43084-142">Implementing connection resiliency</span></span>

1. <span data-ttu-id="43084-143">下载并在 Visual Studio 中打开[WingtipToys](https://go.microsoft.com/fwlink/?LinkID=389434&amp;clcid=0x409)示例 Web 窗体应用程序。</span><span class="sxs-lookup"><span data-stu-id="43084-143">Download and open the [WingtipToys](https://go.microsoft.com/fwlink/?LinkID=389434&amp;clcid=0x409) sample Web Forms application in Visual Studio.</span></span>
2. <span data-ttu-id="43084-144">在**WingtipToys**应用程序的*逻辑*文件夹中，添加名为*WingtipToysConfiguration.cs*的类文件。</span><span class="sxs-lookup"><span data-stu-id="43084-144">In the *Logic* folder of the **WingtipToys** application, add a class file named *WingtipToysConfiguration.cs*.</span></span>
3. <span data-ttu-id="43084-145">用下面的代码替换现有代码：</span><span class="sxs-lookup"><span data-stu-id="43084-145">Replace the existing code with the following code:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample1.cs)]

<span data-ttu-id="43084-146">实体框架会自动运行它在从 `DbConfiguration`派生的类中查找的代码。</span><span class="sxs-lookup"><span data-stu-id="43084-146">The Entity Framework automatically runs the code it finds in a class that derives from `DbConfiguration`.</span></span> <span data-ttu-id="43084-147">您可以使用 `DbConfiguration` 类在代码中执行配置任务，*您将在 web.config 文件中*执行此操作。</span><span class="sxs-lookup"><span data-stu-id="43084-147">You can use the `DbConfiguration` class to do configuration tasks in code that you would otherwise do in the *Web.config* file.</span></span> <span data-ttu-id="43084-148">有关详细信息，请参阅[EntityFramework 基于代码的配置](https://msdn.microsoft.com/data/jj680699)。</span><span class="sxs-lookup"><span data-stu-id="43084-148">For more information, see [EntityFramework Code-Based Configuration](https://msdn.microsoft.com/data/jj680699).</span></span>

1. <span data-ttu-id="43084-149">在*逻辑*文件夹中，打开*AddProducts.cs*文件。</span><span class="sxs-lookup"><span data-stu-id="43084-149">In the *Logic* folder, open the *AddProducts.cs* file.</span></span>
2. <span data-ttu-id="43084-150">为 `System.Data.Entity.Infrastructure` 添加 `using` 语句，如黄色突出显示：</span><span class="sxs-lookup"><span data-stu-id="43084-150">Add a `using` statement for `System.Data.Entity.Infrastructure` as shown highlighted in yellow:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample2.cs?highlight=6)]
3. <span data-ttu-id="43084-151">将 `catch` 块添加到 `AddProduct` 方法，以便将 `RetryLimitExceededException` 记录为黄色突出显示的部分：</span><span class="sxs-lookup"><span data-stu-id="43084-151">Add a `catch` block to the `AddProduct` method so that the `RetryLimitExceededException` is logged as highlighted in yellow:</span></span>   

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample3.cs?highlight=14-15,17-22)]

<span data-ttu-id="43084-152">通过添加 `RetryLimitExceededException` 异常，可以提供更好的日志记录或向用户显示一条错误消息，用户可在其中选择重试该过程。</span><span class="sxs-lookup"><span data-stu-id="43084-152">By adding the `RetryLimitExceededException` exception, you can provide better logging or display an error message to the user where they can choose to try the process again.</span></span> <span data-ttu-id="43084-153">捕获 `RetryLimitExceededException` 异常后，只需尝试并失败几次，就可能会发生暂时性错误。</span><span class="sxs-lookup"><span data-stu-id="43084-153">By catching the `RetryLimitExceededException` exception, the only errors likely to be transient will already have been tried and failed several times.</span></span> <span data-ttu-id="43084-154">返回的实际异常将包装在 `RetryLimitExceededException` 异常中。</span><span class="sxs-lookup"><span data-stu-id="43084-154">The actual exception returned will be wrapped in the `RetryLimitExceededException` exception.</span></span> <span data-ttu-id="43084-155">此外，还添加了一个常规 catch 块。</span><span class="sxs-lookup"><span data-stu-id="43084-155">In addition, you also added a general catch block.</span></span> <span data-ttu-id="43084-156">有关 `RetryLimitExceededException` 异常的详细信息，请参阅[实体框架连接复原/重试逻辑](https://msdn.microsoft.com/data/dn456835)。</span><span class="sxs-lookup"><span data-stu-id="43084-156">For more information about the `RetryLimitExceededException` exception, see [Entity Framework Connection Resiliency / Retry Logic](https://msdn.microsoft.com/data/dn456835).</span></span>

## <a name="command-interception"></a><span data-ttu-id="43084-157">命令侦听</span><span class="sxs-lookup"><span data-stu-id="43084-157">Command interception</span></span>

<span data-ttu-id="43084-158">现在，你已启用重试策略，你如何进行测试来验证它是否按预期方式工作？</span><span class="sxs-lookup"><span data-stu-id="43084-158">Now that you've turned on a retry policy, how do you test to verify that it is working as expected?</span></span> <span data-ttu-id="43084-159">不太容易强制发生暂时性错误，尤其是在本地运行时，将实际暂时性错误集成到自动单元测试中会特别困难。</span><span class="sxs-lookup"><span data-stu-id="43084-159">It's not so easy to force a transient error to happen, especially when you're running locally, and it would be especially difficult to integrate actual transient errors into an automated unit test.</span></span> <span data-ttu-id="43084-160">若要测试连接复原功能，需要有一种方法来截获实体框架发送到 SQL Server 的查询，并将 SQL Server 响应替换为通常是暂时性的异常类型。</span><span class="sxs-lookup"><span data-stu-id="43084-160">To test the connection resiliency feature, you need a way to intercept queries that Entity Framework sends to SQL Server and replace the SQL Server response with an exception type that is typically transient.</span></span>

<span data-ttu-id="43084-161">你还可以使用查询拦截来实现云应用程序的最佳做法：记录对外部服务（如数据库服务）的所有调用的延迟、成功或失败。</span><span class="sxs-lookup"><span data-stu-id="43084-161">You can also use query interception in order to implement a best practice for cloud applications: log the latency and success or failure of all calls to external services such as database services.</span></span>

<span data-ttu-id="43084-162">在本教程的此部分中，你将使用实体框架的[*拦截功能*](https://msdn.microsoft.com/data/dn469464)来记录和模拟暂时性错误。</span><span class="sxs-lookup"><span data-stu-id="43084-162">In this section of the tutorial you'll use the Entity Framework's [*interception feature*](https://msdn.microsoft.com/data/dn469464) both for logging and for simulating transient errors.</span></span>

### <a name="create-a-logging-interface-and-class"></a><span data-ttu-id="43084-163">创建日志记录接口和类</span><span class="sxs-lookup"><span data-stu-id="43084-163">Create a logging interface and class</span></span>

<span data-ttu-id="43084-164">日志记录的最佳做法是使用[`interface`](https://msdn.microsoft.com/library/ms173156.aspx) ，而不是对 `System.Diagnostics.Trace` 或日志记录类的硬编码调用。</span><span class="sxs-lookup"><span data-stu-id="43084-164">A best practice for logging is to do it by using an [`interface`](https://msdn.microsoft.com/library/ms173156.aspx) rather than hard-coding calls to `System.Diagnostics.Trace` or a logging class.</span></span> <span data-ttu-id="43084-165">这样就可以在以后需要时更轻松地更改日志记录机制。</span><span class="sxs-lookup"><span data-stu-id="43084-165">That makes it easier to change your logging mechanism later if you ever need to do that.</span></span> <span data-ttu-id="43084-166">因此，在本部分中，你将创建日志记录接口和类以实现它。</span><span class="sxs-lookup"><span data-stu-id="43084-166">So in this section, you'll create the logging interface and a class to implement it.</span></span>

<span data-ttu-id="43084-167">根据上述过程，已在 Visual Studio 中下载并打开了**WingtipToys**示例应用程序。</span><span class="sxs-lookup"><span data-stu-id="43084-167">Based on the above procedure, you have downloaded and opened the **WingtipToys** sample application in Visual Studio.</span></span>

1. <span data-ttu-id="43084-168">在**WingtipToys**项目中创建一个文件夹并将其命名为*日志记录*。</span><span class="sxs-lookup"><span data-stu-id="43084-168">Create a folder in the **WingtipToys** project and name it *Logging*.</span></span>
2. <span data-ttu-id="43084-169">在*日志记录*文件夹中，创建一个名为*ILogger.cs*的类文件，并将默认代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="43084-169">In the *Logging* folder, create a class file named *ILogger.cs* and replace the default code with the following code:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample4.cs)]

   <span data-ttu-id="43084-170">接口提供三个跟踪级别来指示日志的相对重要性，另一个设计用于为外部服务调用（如数据库查询）提供滞后时间信息。</span><span class="sxs-lookup"><span data-stu-id="43084-170">The interface provides three tracing levels to indicate the relative importance of logs, and one designed to provide latency information for external service calls such as database queries.</span></span> <span data-ttu-id="43084-171">日志记录方法包含允许您在异常中传递的重载。</span><span class="sxs-lookup"><span data-stu-id="43084-171">The logging methods have overloads that let you pass in an exception.</span></span> <span data-ttu-id="43084-172">这是因为实现接口的类可以可靠地记录包括堆栈跟踪和内部异常的异常信息，而不是依赖于整个应用程序的每个日志记录方法调用中执行的操作。</span><span class="sxs-lookup"><span data-stu-id="43084-172">This is so that exception information including stack trace and inner exceptions is reliably logged by the class that implements the interface, instead of relying on that being done in each logging method call throughout the application.</span></span>  
  
   <span data-ttu-id="43084-173">使用 `TraceApi` 方法可以跟踪对外部服务（如 SQL 数据库）的每个调用的延迟。</span><span class="sxs-lookup"><span data-stu-id="43084-173">The `TraceApi` methods enable you to track the latency of each call to an external service such as SQL Database.</span></span>
3. <span data-ttu-id="43084-174">在*日志记录*文件夹中，创建一个名为*Logger.cs*的类文件，并将默认代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="43084-174">In the *Logging* folder, create a class file named *Logger.cs* and replace the default code with the following code:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample5.cs)]

<span data-ttu-id="43084-175">实现使用 `System.Diagnostics` 进行跟踪。</span><span class="sxs-lookup"><span data-stu-id="43084-175">The implementation uses `System.Diagnostics` to do the tracing.</span></span> <span data-ttu-id="43084-176">这是 .NET 的内置功能，可轻松生成和使用跟踪信息。</span><span class="sxs-lookup"><span data-stu-id="43084-176">This is a built-in feature of .NET which makes it easy to generate and use tracing information.</span></span> <span data-ttu-id="43084-177">可以将许多 &quot;侦听器&quot; 用于 `System.Diagnostics` 跟踪、将日志写入文件，或者将日志写入 Windows Azure 中的 blob 存储。</span><span class="sxs-lookup"><span data-stu-id="43084-177">There are many &quot;listeners&quot; you can use with `System.Diagnostics` tracing, to write logs to files, for example, or to write them to blob storage in Windows Azure.</span></span> <span data-ttu-id="43084-178">有关详细信息，请参阅[Visual Studio 中的 Microsoft Azure 网站疑难解答](https://docs.microsoft.com/azure/app-service-web/web-sites-dotnet-troubleshoot-visual-studio)中的一些选项和其他资源的链接。</span><span class="sxs-lookup"><span data-stu-id="43084-178">See some of the options, and links to other resources for more information, in [Troubleshooting Windows Azure Web Sites in Visual Studio](https://docs.microsoft.com/azure/app-service-web/web-sites-dotnet-troubleshoot-visual-studio).</span></span> <span data-ttu-id="43084-179">对于本教程，只需在 Visual Studio 的 "**输出**" 窗口中查看日志。</span><span class="sxs-lookup"><span data-stu-id="43084-179">For this tutorial, you'll only look at logs in the Visual Studio **Output** window.</span></span>

<span data-ttu-id="43084-180">在生产应用程序中，您可能需要考虑使用除 `System.Diagnostics`以外的跟踪框架，如果您决定执行该操作，则可以使用 `ILogger` 接口，使其相对容易地切换到不同的跟踪机制。</span><span class="sxs-lookup"><span data-stu-id="43084-180">In a production application you might want to consider using tracing frameworks other than `System.Diagnostics`, and the `ILogger` interface makes it relatively easy to switch to a different tracing mechanism if you decide to do that.</span></span>

### <a name="create-interceptor-classes"></a><span data-ttu-id="43084-181">创建侦听器类</span><span class="sxs-lookup"><span data-stu-id="43084-181">Create interceptor classes</span></span>

<span data-ttu-id="43084-182">接下来，您将创建实体框架将在每次将查询发送到数据库时调用的类，一个用于模拟暂时性错误，另一个用于执行日志记录。</span><span class="sxs-lookup"><span data-stu-id="43084-182">Next, you'll create the classes that the Entity Framework will call into every time it is going to send a query to the database, one to simulate transient errors and one to do logging.</span></span> <span data-ttu-id="43084-183">这些侦听器类必须从 `DbCommandInterceptor` 类派生。</span><span class="sxs-lookup"><span data-stu-id="43084-183">These interceptor classes must derive from the `DbCommandInterceptor` class.</span></span> <span data-ttu-id="43084-184">在这些方法中，你将编写在要执行查询时自动调用的方法重写。</span><span class="sxs-lookup"><span data-stu-id="43084-184">In them, you write method overrides that are automatically called when the query is about to be executed.</span></span> <span data-ttu-id="43084-185">在这些方法中，你可以检查或记录要发送到数据库的查询，并且可以在将查询发送到数据库之前对其进行更改，也可以在不将查询传递到数据库的情况下将内容返回给实体框架。</span><span class="sxs-lookup"><span data-stu-id="43084-185">In these methods you can examine or log the query that is being sent to the database, and you can change the query before it's sent to the database or return something to Entity Framework yourself without even passing the query to the database.</span></span>

1. <span data-ttu-id="43084-186">若要创建侦听器类，以便在将每个 SQL 查询发送到数据库之前对其进行记录，请在*逻辑*文件夹中创建一个名为*InterceptorLogging.cs*的类文件，并将默认代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="43084-186">To create the interceptor class that will log every SQL query before it is sent to the database, create a class file named *InterceptorLogging.cs* in the *Logic* folder and replace the default code with the following code:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample6.cs)]

   <span data-ttu-id="43084-187">对于成功的查询或命令，此代码会写入包含延迟信息的信息日志。</span><span class="sxs-lookup"><span data-stu-id="43084-187">For successful queries or commands, this code writes an Information log with latency information.</span></span> <span data-ttu-id="43084-188">对于异常，它将创建错误日志。</span><span class="sxs-lookup"><span data-stu-id="43084-188">For exceptions, it creates an Error log.</span></span>
2. <span data-ttu-id="43084-189">若要创建将在名为*AdminPage*的页面上的 "**名称**" 文本框中输入 &quot;Throw&quot; 时生成虚拟暂时性错误的侦听器类，请在*逻辑*文件夹中创建一个名为*InterceptorTransientErrors.cs*的类文件，并将默认代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="43084-189">To create the interceptor class that will generate dummy transient errors when you enter &quot;Throw&quot; in the **Name** textbox on the page named *AdminPage.aspx*, create a class file named *InterceptorTransientErrors.cs* in the *Logic* folder and replace the default code with the following code:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample7.cs)]

    <span data-ttu-id="43084-190">此代码仅覆盖 `ReaderExecuting` 方法，该方法将为可以返回多行数据的查询调用。</span><span class="sxs-lookup"><span data-stu-id="43084-190">This code only overrides the `ReaderExecuting` method, which is called for queries that can return multiple rows of data.</span></span> <span data-ttu-id="43084-191">如果要检查其他类型查询的连接复原能力，还可以重写 `NonQueryExecuting` 和 `ScalarExecuting` 方法，因为日志记录侦听器会这样做。</span><span class="sxs-lookup"><span data-stu-id="43084-191">If you wanted to check connection resiliency for other types of queries, you could also override the `NonQueryExecuting` and `ScalarExecuting` methods, as the logging interceptor does.</span></span>  
  
   <span data-ttu-id="43084-192">稍后，你将以 "管理员" 身份登录，并选择顶部导航栏上的 "**管理员**" 链接。</span><span class="sxs-lookup"><span data-stu-id="43084-192">Later, you will log in as the "Admin" and select the **Admin** link on the top navigation bar.</span></span> <span data-ttu-id="43084-193">然后，在 " *AdminPage* " 页上，你将添加一个名为 &quot;Throw&quot;的产品。</span><span class="sxs-lookup"><span data-stu-id="43084-193">Then, on the *AdminPage.aspx* page you will add a product named &quot;Throw&quot;.</span></span> <span data-ttu-id="43084-194">此代码为错误编号20创建了一个虚拟 SQL 数据库异常，这是一个已知通常为暂时性的类型。</span><span class="sxs-lookup"><span data-stu-id="43084-194">The code creates a dummy SQL Database exception for error number 20, a type known to be typically transient.</span></span> <span data-ttu-id="43084-195">目前识别为暂时性的其他错误号为64、233、10053、10054、10060、10928、10929、40197、40501和40613，但在 SQL 数据库的新版本中可能会有所更改。</span><span class="sxs-lookup"><span data-stu-id="43084-195">Other error numbers currently recognized as transient are 64, 233, 10053, 10054, 10060, 10928, 10929, 40197, 40501, and 40613, but these are subject to change in new versions of SQL Database.</span></span> <span data-ttu-id="43084-196">该产品将重命名为 "TransientErrorExample"，你可以在*InterceptorTransientErrors.cs*文件的代码中执行此操作。</span><span class="sxs-lookup"><span data-stu-id="43084-196">The product will be renamed to "TransientErrorExample", which you can follow in the code of the *InterceptorTransientErrors.cs* file.</span></span>  
  
   <span data-ttu-id="43084-197">此代码会将异常返回到实体框架，而不是运行查询并传递回结果。</span><span class="sxs-lookup"><span data-stu-id="43084-197">The code returns the exception to Entity Framework instead of running the query and passing back results.</span></span> <span data-ttu-id="43084-198">暂时异常返回*四*次，然后代码恢复到将查询传递到数据库的正常过程。</span><span class="sxs-lookup"><span data-stu-id="43084-198">The transient exception is returned *four* times, and then the code reverts to the normal procedure of passing the query to the database.</span></span>

    <span data-ttu-id="43084-199">由于记录了所有内容，因此你将能够看到实体框架尝试在最后一次成功之前执行查询四次，并且应用程序的唯一区别在于，使用查询结果呈现页面需要更长时间。</span><span class="sxs-lookup"><span data-stu-id="43084-199">Because everything is logged, you'll be able to see that Entity Framework tries to execute the query four times before finally succeeding, and the only difference in the application is that it takes longer to render a page with query results.</span></span>  
  
   <span data-ttu-id="43084-200">可以配置实体框架重试的次数;代码指定了四次，因为这是 SQL 数据库执行策略的默认值。</span><span class="sxs-lookup"><span data-stu-id="43084-200">The number of times the Entity Framework will retry is configurable; the code specifies four times because that's the default value for the SQL Database execution policy.</span></span> <span data-ttu-id="43084-201">如果更改执行策略，则还需更改此处的代码，用于指定生成暂时性错误的次数。</span><span class="sxs-lookup"><span data-stu-id="43084-201">If you change the execution policy, you'd also change the code here that specifies how many times transient errors are generated.</span></span> <span data-ttu-id="43084-202">你还可以更改代码以生成更多异常，以便实体框架将引发 `RetryLimitExceededException` 异常。</span><span class="sxs-lookup"><span data-stu-id="43084-202">You could also change the code to generate more exceptions so that Entity Framework will throw the `RetryLimitExceededException` exception.</span></span>
3. <span data-ttu-id="43084-203">在*global.asax*中，添加以下 using 语句：</span><span class="sxs-lookup"><span data-stu-id="43084-203">In *Global.asax*, add the following using statements:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample8.cs)]
4. <span data-ttu-id="43084-204">然后，将突出显示的行添加到 `Application_Start` 方法：</span><span class="sxs-lookup"><span data-stu-id="43084-204">Then, add the highlighted lines to the `Application_Start` method:</span></span>  

    [!code-csharp[Main](aspnet-web-forms-connection-resiliency-and-command-interception/samples/sample9.cs?highlight=17-20)]

<span data-ttu-id="43084-205">如果实体框架将查询发送到数据库，则会导致侦听器代码运行的代码行。</span><span class="sxs-lookup"><span data-stu-id="43084-205">These lines of code are what causes your interceptor code to be run when Entity Framework sends queries to the database.</span></span> <span data-ttu-id="43084-206">请注意，由于你为暂时性错误模拟和日志记录创建了单独的侦听器类，因此你可以单独启用和禁用它们。</span><span class="sxs-lookup"><span data-stu-id="43084-206">Notice that because you created separate interceptor classes for transient error simulation and logging, you can independently enable and disable them.</span></span>   
  
 <span data-ttu-id="43084-207">可以在代码中的任意位置使用 `DbInterception.Add` 方法添加侦听器。不一定要使用 `Application_Start` 方法。</span><span class="sxs-lookup"><span data-stu-id="43084-207">You can add interceptors using the `DbInterception.Add` method anywhere in your code; it doesn't have to be in the `Application_Start` method.</span></span> <span data-ttu-id="43084-208">另一种方法是，如果未在 `Application_Start` 方法中添加侦听器，将更新或添加名为*WingtipToysConfiguration.cs*的类，并将上面的代码放在 `WingtipToysConfiguration` 类构造函数的末尾。</span><span class="sxs-lookup"><span data-stu-id="43084-208">Another option, if you didn't add interceptors in the `Application_Start` method, would be to update or add the class named *WingtipToysConfiguration.cs* and put the above code at the end of the constructor of the `WingtipToysConfiguration` class.</span></span>

<span data-ttu-id="43084-209">无论你将此代码放在何处，请注意不要多次对同一侦听器执行 `DbInterception.Add`，否则你将获得其他侦听器实例。</span><span class="sxs-lookup"><span data-stu-id="43084-209">Wherever you put this code, be careful not to execute `DbInterception.Add` for the same interceptor more than once, or you'll get additional interceptor instances.</span></span> <span data-ttu-id="43084-210">例如，如果您添加日志记录侦听器两次，您将看到每个 SQL 查询都有两个日志。</span><span class="sxs-lookup"><span data-stu-id="43084-210">For example, if you add the logging interceptor twice, you'll see two logs for every SQL query.</span></span>

<span data-ttu-id="43084-211">拦截按注册顺序执行（调用 `DbInterception.Add` 方法的顺序）。</span><span class="sxs-lookup"><span data-stu-id="43084-211">Interceptors are executed in the order of registration (the order in which the `DbInterception.Add` method is called).</span></span> <span data-ttu-id="43084-212">此顺序可能取决于您在侦听器中所执行的操作。</span><span class="sxs-lookup"><span data-stu-id="43084-212">The order might matter depending on what you're doing in the interceptor.</span></span> <span data-ttu-id="43084-213">例如，侦听器可能会更改它在 `CommandText` 属性中获取的 SQL 命令。</span><span class="sxs-lookup"><span data-stu-id="43084-213">For example, an interceptor might change the SQL command that it gets in the `CommandText` property.</span></span> <span data-ttu-id="43084-214">如果它确实更改了 SQL 命令，则下一个侦听器将获取更改的 SQL 命令，而不是原始 SQL 命令。</span><span class="sxs-lookup"><span data-stu-id="43084-214">If it does change the SQL command, the next interceptor will get the changed SQL command, not the original SQL command.</span></span>

<span data-ttu-id="43084-215">您已编写暂时性错误模拟代码，使您可以通过在用户界面中输入不同的值导致暂时性错误。</span><span class="sxs-lookup"><span data-stu-id="43084-215">You've written the transient error simulation code in a way that lets you cause transient errors by entering a different value in the UI.</span></span> <span data-ttu-id="43084-216">作为替代方法，您可以编写侦听器代码，以便始终在不检查特定参数值的情况下生成暂时性异常序列。</span><span class="sxs-lookup"><span data-stu-id="43084-216">As an alternative, you could write the interceptor code to always generate the sequence of transient exceptions without checking for a particular parameter value.</span></span> <span data-ttu-id="43084-217">然后，只需在要生成暂时性错误时才添加侦听器。</span><span class="sxs-lookup"><span data-stu-id="43084-217">You could then add the interceptor only when you want to generate transient errors.</span></span> <span data-ttu-id="43084-218">但是，如果您这样做，则在数据库初始化完成之前，不要添加侦听器。</span><span class="sxs-lookup"><span data-stu-id="43084-218">If you do this, however, don't add the interceptor until after database initialization has completed.</span></span> <span data-ttu-id="43084-219">换句话说，在开始生成暂时性错误之前，请至少执行一项数据库操作，例如对某个实体集进行查询。</span><span class="sxs-lookup"><span data-stu-id="43084-219">In other words, do at least one database operation such as a query on one of your entity sets before you start generating transient errors.</span></span> <span data-ttu-id="43084-220">实体框架在数据库初始化期间执行多个查询，它们不在事务中执行，因此在初始化期间发生的错误可能导致上下文进入不一致状态。</span><span class="sxs-lookup"><span data-stu-id="43084-220">The Entity Framework executes several queries during database initialization, and they aren't executed in a transaction, so errors during initialization could cause the context to get into an inconsistent state.</span></span>

## <a name="test-logging-and-connection-resiliency"></a><span data-ttu-id="43084-221">测试日志记录和连接复原</span><span class="sxs-lookup"><span data-stu-id="43084-221">Test logging and connection resiliency</span></span>

1. <span data-ttu-id="43084-222">在 Visual Studio 中，按**F5**以调试模式运行应用程序，然后使用 "Pa $ $word" 作为密码以 "管理员" 身份登录。</span><span class="sxs-lookup"><span data-stu-id="43084-222">In Visual Studio, press **F5** to run the application in debug mode, and then login as "Admin" using "Pa$$word" as the password.</span></span>
2. <span data-ttu-id="43084-223">在顶部的导航栏中选择 "**管理员**"。</span><span class="sxs-lookup"><span data-stu-id="43084-223">Select **Admin** from the navigation bar at the top.</span></span>
3. <span data-ttu-id="43084-224">输入具有适当说明、价格和图像文件的名为 "Throw" 的新产品。</span><span class="sxs-lookup"><span data-stu-id="43084-224">Enter a new product named "Throw" with appropriate description, price and image file.</span></span>
4. <span data-ttu-id="43084-225">按 "**添加产品**" 按钮。</span><span class="sxs-lookup"><span data-stu-id="43084-225">Press the **Add Product** button.</span></span>  
   <span data-ttu-id="43084-226">你会注意到，浏览器似乎已挂起几秒钟，而实体框架重试查询多次。</span><span class="sxs-lookup"><span data-stu-id="43084-226">You'll notice that the browser seems to hang for several seconds while Entity Framework is retrying the query several times.</span></span> <span data-ttu-id="43084-227">第一次重试发生的速度非常快，在每次重试前等待增加。</span><span class="sxs-lookup"><span data-stu-id="43084-227">The first retry happens very quickly, then the wait increases before each additional retry.</span></span> <span data-ttu-id="43084-228">每次重试之前等待的这一过程称为*指数回退*。</span><span class="sxs-lookup"><span data-stu-id="43084-228">This process of waiting longer before each retry is called *exponential backoff* .</span></span>
5. <span data-ttu-id="43084-229">等待，直到页面不再尝试加载。</span><span class="sxs-lookup"><span data-stu-id="43084-229">Wait until the page is no longer attempting to load.</span></span>
6. <span data-ttu-id="43084-230">停止项目，并查看 "Visual Studio**输出**" 窗口以查看跟踪输出。</span><span class="sxs-lookup"><span data-stu-id="43084-230">Stop the project and look at the Visual Studio **Output** window to see the tracing output.</span></span> <span data-ttu-id="43084-231">可以通过选择 "**调试**" -&gt; **Windows** -&gt;**输出**来查找 "**输出**" 窗口。</span><span class="sxs-lookup"><span data-stu-id="43084-231">You can find the **Output** window by selecting **Debug** -&gt; **Windows** -&gt; **Output**.</span></span> <span data-ttu-id="43084-232">您可能必须滚动使用记录器编写的其他几个日志。</span><span class="sxs-lookup"><span data-stu-id="43084-232">You might have to scroll past several other logs written by your logger.</span></span>  
  
   <span data-ttu-id="43084-233">请注意，您可以查看发送到数据库的实际 SQL 查询。</span><span class="sxs-lookup"><span data-stu-id="43084-233">Notice that you can see the actual SQL queries sent to the database.</span></span> <span data-ttu-id="43084-234">你将看到一些初始查询和命令，实体框架在开始之前，请检查数据库版本和迁移历史记录表。</span><span class="sxs-lookup"><span data-stu-id="43084-234">You see some initial queries and commands that Entity Framework does to get started, checking the database version and migration history table.</span></span>   
    <span data-ttu-id="43084-235">![输出窗口](aspnet-web-forms-connection-resiliency-and-command-interception/_static/image1.png) </span><span class="sxs-lookup"><span data-stu-id="43084-235">![Output Window](aspnet-web-forms-connection-resiliency-and-command-interception/_static/image1.png) </span></span>  
   <span data-ttu-id="43084-236">请注意，除非停止并重新启动应用程序，否则不能重复此测试。</span><span class="sxs-lookup"><span data-stu-id="43084-236">Note that you can't repeat this test unless you stop the application and restart it.</span></span> <span data-ttu-id="43084-237">如果希望能够在应用程序的单个运行中多次测试连接复原，可以编写代码以在 `InterceptorTransientErrors` 中重置错误计数器。</span><span class="sxs-lookup"><span data-stu-id="43084-237">If you wanted to be able to test connection resiliency multiple times in a single run of the application, you could write code to reset the error counter in `InterceptorTransientErrors` .</span></span>
7. <span data-ttu-id="43084-238">若要查看执行策略（重试策略）的差异，请在*逻辑*文件夹中注释掉*WingtipToysConfiguration.cs*文件中的 `SetExecutionStrategy` 行，再次运行调试模式中的 "**管理**" 页，然后添加名为 &quot;的产品&quot; 再次引发。</span><span class="sxs-lookup"><span data-stu-id="43084-238">To see the difference the execution strategy (retry policy) makes, comment out the `SetExecutionStrategy` line in *WingtipToysConfiguration.cs* file in the *Logic* folder, run the **Admin** page in debug mode again, and add the product named &quot;Throw&quot; again.</span></span>  
  
   <span data-ttu-id="43084-239">这次当首次尝试执行查询时，调试器会立即停止第一个生成的异常。</span><span class="sxs-lookup"><span data-stu-id="43084-239">This time the debugger stops on the first generated exception immediately when it tries to execute the query the first time.</span></span>  
    ![调试-查看详细信息](aspnet-web-forms-connection-resiliency-and-command-interception/_static/image2.png)
8. <span data-ttu-id="43084-241">取消注释*WingtipToysConfiguration.cs*文件中的 `SetExecutionStrategy` 行。</span><span class="sxs-lookup"><span data-stu-id="43084-241">Uncomment the `SetExecutionStrategy` line in the *WingtipToysConfiguration.cs* file.</span></span>

## <a name="summary"></a><span data-ttu-id="43084-242">摘要</span><span class="sxs-lookup"><span data-stu-id="43084-242">Summary</span></span>

<span data-ttu-id="43084-243">在本教程中，你已了解如何修改 Web 窗体示例应用程序，以支持连接复原和命令截获。</span><span class="sxs-lookup"><span data-stu-id="43084-243">In this tutorial you've seen how to modify a Web Forms sample application to support connection resiliency and command interception.</span></span>

## <a name="next-steps"></a><span data-ttu-id="43084-244">后续步骤</span><span class="sxs-lookup"><span data-stu-id="43084-244">Next Steps</span></span>

<span data-ttu-id="43084-245">在 ASP.NET Web 窗体中查看了连接复原和命令截获后，请查看[ASP.NET 4.5 中](../performance-and-caching/using-asynchronous-methods-in-aspnet-45.md)的 ASP.NET Web 窗体主题异步方法。</span><span class="sxs-lookup"><span data-stu-id="43084-245">After you have reviewed connection resiliency and command interception in ASP.NET Web Forms, review the ASP.NET Web Forms topic [Asynchronous Methods in ASP.NET 4.5](../performance-and-caching/using-asynchronous-methods-in-aspnet-45.md).</span></span> <span data-ttu-id="43084-246">本主题将介绍使用 Visual Studio 生成异步 ASP.NET Web 窗体应用程序的基础知识。</span><span class="sxs-lookup"><span data-stu-id="43084-246">The topic will teach you the basics of building an asynchronous ASP.NET Web Forms application using Visual Studio.</span></span>
