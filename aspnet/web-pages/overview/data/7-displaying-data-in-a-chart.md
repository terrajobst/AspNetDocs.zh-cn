---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: 使用 ASP.NET 网页（Razor）在图表中显示数据 |Microsoft Docs
author: microsoft
description: 本章介绍如何在图表中显示数据。 在前面的章节中，已学习了如何手动和在网格中显示数据。 本章介绍 。
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 6dad67d4e3d38d57a761c567d937d714a3184ea9
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/06/2020
ms.locfileid: "78509132"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="9ffff-105">使用 ASP.NET 网页（Razor）在图表中显示数据</span><span class="sxs-lookup"><span data-stu-id="9ffff-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="9ffff-106">由[Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="9ffff-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="9ffff-107">本文介绍如何使用 "`Chart` 帮助程序" 在 ASP.NET 网页（Razor）网站中显示数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="9ffff-108">**你将学习的内容**：</span><span class="sxs-lookup"><span data-stu-id="9ffff-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="9ffff-109">如何在图表中显示数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="9ffff-110">如何使用内置主题为图表建立样式。</span><span class="sxs-lookup"><span data-stu-id="9ffff-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="9ffff-111">如何保存图表以及如何缓存它们以获得更好的性能。</span><span class="sxs-lookup"><span data-stu-id="9ffff-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="9ffff-112">下面是本文中引入的 ASP.NET 编程功能：</span><span class="sxs-lookup"><span data-stu-id="9ffff-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="9ffff-113">`Chart` 帮助程序。</span><span class="sxs-lookup"><span data-stu-id="9ffff-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="9ffff-114">本文中的信息适用于 ASP.NET 网页1.0 和网页2。</span><span class="sxs-lookup"><span data-stu-id="9ffff-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="9ffff-115">图表帮助器</span><span class="sxs-lookup"><span data-stu-id="9ffff-115">The Chart Helper</span></span>

<span data-ttu-id="9ffff-116">如果要以图形形式显示数据，可以使用 `Chart` 帮助程序。</span><span class="sxs-lookup"><span data-stu-id="9ffff-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="9ffff-117">`Chart` 帮助程序可以呈现图像，该图像显示各种图表类型的数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="9ffff-118">它支持多种格式和标签选项。</span><span class="sxs-lookup"><span data-stu-id="9ffff-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="9ffff-119">`Chart` helper 可以呈现30多种类型的图表，包括您可能熟悉的 Microsoft Excel 或其他工具&#8212;区域图表、条形图、柱形图、折线图和饼图的所有类型的图表，以及股价图之类的更专业化的图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="9ffff-120">**面积图**![说明：面积图类型的图片](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="9ffff-121">**条形图**![说明：条形图类型的图片](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="9ffff-122">**柱形图**![说明：柱形图类型的图片](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="9ffff-123">**折线图**![说明：折线图类型的图片](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="9ffff-124">**饼图**![说明：饼图类型的图片](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="9ffff-125">**股价图**![说明：股价图类型的图片](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="9ffff-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="9ffff-126">图表元素</span><span class="sxs-lookup"><span data-stu-id="9ffff-126">Chart Elements</span></span>

<span data-ttu-id="9ffff-127">图表显示数据和其他元素（如图例、轴、序列等）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="9ffff-128">下图显示了在使用 `Chart` 帮助程序时可以自定义的许多图表元素。</span><span class="sxs-lookup"><span data-stu-id="9ffff-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="9ffff-129">本文介绍如何设置这些元素的部分（而非全部）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-129">This article shows you how to set some (not all) of these elements.</span></span>

![说明：显示图表元素的图片](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="9ffff-131">从数据创建图表</span><span class="sxs-lookup"><span data-stu-id="9ffff-131">Creating a Chart from Data</span></span>

<span data-ttu-id="9ffff-132">在图表中显示的数据可以来自数组、从数据库返回的结果或 XML 文件中的数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="9ffff-133">使用数组</span><span class="sxs-lookup"><span data-stu-id="9ffff-133">Using an Array</span></span>

<span data-ttu-id="9ffff-134">如[使用 Razor 语法 ASP.NET 网页编程](https://go.microsoft.com/fwlink/?LinkId=202890)中所述，数组允许您在单个变量中存储类似项的集合。</span><span class="sxs-lookup"><span data-stu-id="9ffff-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="9ffff-135">您可以使用数组来包含要包含在图表中的数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="9ffff-136">此过程说明如何使用默认图表类型，通过数组中的数据创建图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="9ffff-137">它还演示如何在页面中显示图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="9ffff-138">创建名为*ChartArrayBasic*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="9ffff-139">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="9ffff-140">代码首先创建一个新图表并设置其宽度和高度。</span><span class="sxs-lookup"><span data-stu-id="9ffff-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="9ffff-141">使用 `AddTitle` 方法指定图表标题。</span><span class="sxs-lookup"><span data-stu-id="9ffff-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="9ffff-142">若要添加数据，请使用 `AddSeries` 方法。</span><span class="sxs-lookup"><span data-stu-id="9ffff-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="9ffff-143">在此示例中，将使用 `AddSeries` 方法的 `name`、`xValue`和 `yValues` 参数。</span><span class="sxs-lookup"><span data-stu-id="9ffff-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="9ffff-144">`name` 参数将显示在图表图例中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="9ffff-145">`xValue` 参数包含沿图表水平轴显示的数据数组。</span><span class="sxs-lookup"><span data-stu-id="9ffff-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="9ffff-146">`yValues` 参数包含用于绘制图表垂直点的数据数组。</span><span class="sxs-lookup"><span data-stu-id="9ffff-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="9ffff-147">`Write` 方法实际上会呈现图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="9ffff-148">在这种情况下，由于未指定图表类型，`Chart` 帮助器将呈现其默认图表，这是一个柱形图。</span><span class="sxs-lookup"><span data-stu-id="9ffff-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="9ffff-149">在浏览器中运行页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-149">Run the page in the browser.</span></span> <span data-ttu-id="9ffff-150">浏览器将显示图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="9ffff-151">对图表数据使用数据库查询</span><span class="sxs-lookup"><span data-stu-id="9ffff-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="9ffff-152">如果您想要图表的信息位于数据库中，则可以运行数据库查询，然后使用结果中的数据创建图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="9ffff-153">此过程说明如何读取和显示有关在[ASP.NET 网页站点中使用数据库的简介](https://go.microsoft.com/fwlink/?LinkId=202893)中创建的数据库的数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="9ffff-154">如果文件夹尚不存在，则将*应用\_Data*文件夹添加到网站的根目录。</span><span class="sxs-lookup"><span data-stu-id="9ffff-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="9ffff-155">在*应用\_Data*文件夹中，添加使用[ASP.NET 网页站点中的数据库简介](https://go.microsoft.com/fwlink/?LinkId=202893)中描述的名为 SmallBakery 的数据库文件 *。*</span><span class="sxs-lookup"><span data-stu-id="9ffff-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="9ffff-156">创建名为*ChartDataQuery*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="9ffff-157">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="9ffff-158">代码首先打开 SmallBakery 数据库，并将其分配给名为 `db`的变量。</span><span class="sxs-lookup"><span data-stu-id="9ffff-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="9ffff-159">此变量表示可用于读取和写入数据库的 `Database` 对象。</span><span class="sxs-lookup"><span data-stu-id="9ffff-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="9ffff-160">接下来，代码将运行 SQL 查询，以获取每个产品的名称和价格。</span><span class="sxs-lookup"><span data-stu-id="9ffff-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="9ffff-161">该代码将创建一个新的图表，并通过调用图表的 `DataBindTable` 方法，将数据库查询传递给它。</span><span class="sxs-lookup"><span data-stu-id="9ffff-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="9ffff-162">此方法采用两个参数： `dataSource` 参数用于查询中的数据，而 `xField` 参数用于设置用于图表的 x 轴的数据列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="9ffff-163">作为使用 `DataBindTable` 方法的替代方法，您可以使用 `Chart` 帮助器的 `AddSeries` 方法。</span><span class="sxs-lookup"><span data-stu-id="9ffff-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="9ffff-164">利用 `AddSeries` 方法，你可以设置 `xValue` 和 `yValues` 参数。</span><span class="sxs-lookup"><span data-stu-id="9ffff-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="9ffff-165">例如，不使用如下所示的 `DataBindTable` 方法：</span><span class="sxs-lookup"><span data-stu-id="9ffff-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="9ffff-166">可以使用如下所示的 `AddSeries` 方法：</span><span class="sxs-lookup"><span data-stu-id="9ffff-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="9ffff-167">两者都呈现相同的结果。</span><span class="sxs-lookup"><span data-stu-id="9ffff-167">Both render the same results.</span></span> <span data-ttu-id="9ffff-168">`AddSeries` 方法更灵活，因为你可以更明确地指定图表类型和数据，但如果不需要额外的灵活性，则 `DataBindTable` 方法更易于使用。</span><span class="sxs-lookup"><span data-stu-id="9ffff-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="9ffff-169">在浏览器中运行页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="9ffff-170">使用 XML 数据</span><span class="sxs-lookup"><span data-stu-id="9ffff-170">Using XML Data</span></span>

<span data-ttu-id="9ffff-171">用于绘制图表的第三个选项是使用 XML 文件作为图表的数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="9ffff-172">这要求 XML 文件还具有描述 XML 结构的架构文件（ *.xsd*文件）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="9ffff-173">此过程演示如何从 XML 文件读取数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="9ffff-174">在*应用\_data* "文件夹中，创建一个名为" *data .xml*"的新 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="9ffff-175">将现有的 XML 替换为以下项，这是有关虚构公司中的员工的一些 XML 数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="9ffff-176">在*应用\_data*文件夹中，创建一个名为 " *data .xsd*" 的新 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="9ffff-177">（请注意，此时间的扩展名为 *.xsd*。）</span><span class="sxs-lookup"><span data-stu-id="9ffff-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="9ffff-178">将现有的 XML 替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="9ffff-179">在网站的根目录中，创建一个名为*ChartDataXML*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="9ffff-180">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="9ffff-181">代码首先创建一个 `DataSet` 对象。</span><span class="sxs-lookup"><span data-stu-id="9ffff-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="9ffff-182">此对象用于管理从 XML 文件中读取的数据，并根据架构文件中的信息对其进行组织。</span><span class="sxs-lookup"><span data-stu-id="9ffff-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="9ffff-183">（请注意，代码顶部包含语句 `using SystemData`。</span><span class="sxs-lookup"><span data-stu-id="9ffff-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="9ffff-184">这是为了能够处理 `DataSet` 对象所必需的。</span><span class="sxs-lookup"><span data-stu-id="9ffff-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="9ffff-185">有关详细信息，请参阅本文后面的[&quot;使用&quot; 语句和完全限定的名称](#SB_UsingStatements)。）</span><span class="sxs-lookup"><span data-stu-id="9ffff-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="9ffff-186">接下来，该代码基于数据集创建一个 `DataView` 对象。</span><span class="sxs-lookup"><span data-stu-id="9ffff-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="9ffff-187">数据视图提供了一个图表可以绑定到&#8212;的对象，该对象是读取和绘制的。</span><span class="sxs-lookup"><span data-stu-id="9ffff-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="9ffff-188">图表将使用 `AddSeries` 方法绑定到数据，正如你之前在对数组数据进行图表时看到的一样，只是这次 `xValue` 和 `yValues` 参数设置为 `DataView` 对象。</span><span class="sxs-lookup"><span data-stu-id="9ffff-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="9ffff-189">此示例还演示如何指定特定图表类型。</span><span class="sxs-lookup"><span data-stu-id="9ffff-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="9ffff-190">在 `AddSeries` 方法中添加数据时，还会将 `chartType` 参数设置为显示饼图。</span><span class="sxs-lookup"><span data-stu-id="9ffff-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="9ffff-191">在浏览器中运行页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="9ffff-192">"Using" 语句和完全限定名称</span><span class="sxs-lookup"><span data-stu-id="9ffff-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="9ffff-193">与 Razor 语法 ASP.NET 网页的 .NET Framework 基于包含数千个组件（类）的。</span><span class="sxs-lookup"><span data-stu-id="9ffff-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="9ffff-194">为了使其可用于处理所有这些类，它们被组织到一些*命名空间*中，这些命名空间有点像库。</span><span class="sxs-lookup"><span data-stu-id="9ffff-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="9ffff-195">例如，`System.Web` 命名空间包含支持浏览器/服务器通信的类，`System.Xml` 命名空间包含用于创建和读取 XML 文件的类，`System.Data` 命名空间包含可用于处理数据的类。</span><span class="sxs-lookup"><span data-stu-id="9ffff-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="9ffff-196">为了访问 .NET Framework 中的任何给定类，代码不仅需要知道类名称，还需要知道类所在的命名空间。</span><span class="sxs-lookup"><span data-stu-id="9ffff-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="9ffff-197">例如，为了使用 `Chart` 帮助程序，代码需要查找 `System.Web.Helpers.Chart` 类，该类将命名空间（`System.Web.Helpers`）与类名称（`Chart`）组合在一起。</span><span class="sxs-lookup"><span data-stu-id="9ffff-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="9ffff-198">这称为类的*完全限定*名称&#8212; ，它在 .NET Framework 的 vastness 中具有完整的明确位置。</span><span class="sxs-lookup"><span data-stu-id="9ffff-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="9ffff-199">在代码中，这将如下所示：</span><span class="sxs-lookup"><span data-stu-id="9ffff-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="9ffff-200">然而，每次需要引用一个类或帮助器时，都必须使用这些长的、完全限定的名称，这是一项很繁琐的（并且容易出错）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="9ffff-201">因此，为了更轻松地使用类名称，你可以*导入*感兴趣的命名空间，这通常只是 .NET Framework 中的多个命名空间中的少数几个。</span><span class="sxs-lookup"><span data-stu-id="9ffff-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="9ffff-202">如果导入了命名空间，则只能使用类名称（`Chart`）而不是完全限定的名称（`System.Web.Helpers.Chart`）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="9ffff-203">当你的代码运行并遇到类名称时，它可以仅查看已导入的命名空间以找到该类。</span><span class="sxs-lookup"><span data-stu-id="9ffff-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="9ffff-204">使用 Razor 语法的 ASP.NET 网页创建网页时，通常每次使用相同的一组类，包括 `WebPage` 类、各种帮助程序等。</span><span class="sxs-lookup"><span data-stu-id="9ffff-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="9ffff-205">为了节省每次创建网站时导入相关命名空间的工作，ASP.NET 配置为每个网站自动导入一组核心命名空间。</span><span class="sxs-lookup"><span data-stu-id="9ffff-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="9ffff-206">这就是你不必处理命名空间或导入的原因，你曾经处理过的所有类都在已为你导入的命名空间中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="9ffff-207">但是，有时你需要使用不在自动导入的命名空间中的类。</span><span class="sxs-lookup"><span data-stu-id="9ffff-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="9ffff-208">在这种情况下，可以使用该类的完全限定名称，也可以手动导入包含该类的命名空间。</span><span class="sxs-lookup"><span data-stu-id="9ffff-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="9ffff-209">若要导入命名空间，请使用 `using` 语句（`import` 在 Visual Basic 中），如上文所述。</span><span class="sxs-lookup"><span data-stu-id="9ffff-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="9ffff-210">例如，`DataSet` 类在 `System.Data` 命名空间中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="9ffff-211">`System.Data` 命名空间不会自动用于 ASP.NET Razor 页面。</span><span class="sxs-lookup"><span data-stu-id="9ffff-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="9ffff-212">因此，若要使用其完全限定名称来处理 `DataSet` 类，可以使用如下所示的代码：</span><span class="sxs-lookup"><span data-stu-id="9ffff-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="9ffff-213">如果必须重复使用 `DataSet` 类，可以导入如下所示的命名空间，然后在代码中只使用类名称：</span><span class="sxs-lookup"><span data-stu-id="9ffff-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="9ffff-214">您可以为要引用的任何其他 .NET Framework 命名空间添加 `using` 语句。</span><span class="sxs-lookup"><span data-stu-id="9ffff-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="9ffff-215">但正如前面所述，你不需要经常执行此操作，因为你将使用的大多数类都位于 ASP.NET 自动导入的命名空间中，以便在 *.* # 和*vbhtml*页中使用。</span><span class="sxs-lookup"><span data-stu-id="9ffff-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="9ffff-216">在网页中显示图表</span><span class="sxs-lookup"><span data-stu-id="9ffff-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="9ffff-217">在到目前为止，您已经看到了一个图表，然后将该图表作为图形直接呈现到浏览器。</span><span class="sxs-lookup"><span data-stu-id="9ffff-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="9ffff-218">但在许多情况下，你希望将图表显示为页的一部分，而不只是在浏览器中显示。</span><span class="sxs-lookup"><span data-stu-id="9ffff-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="9ffff-219">若要执行此操作，需要一个两步过程。</span><span class="sxs-lookup"><span data-stu-id="9ffff-219">To do that requires a two-step process.</span></span> <span data-ttu-id="9ffff-220">第一步是创建一个生成图表的页面，正如您所看到的那样。</span><span class="sxs-lookup"><span data-stu-id="9ffff-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="9ffff-221">第二步是在另一页中显示生成的图像。</span><span class="sxs-lookup"><span data-stu-id="9ffff-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="9ffff-222">若要显示图像，请使用 HTML `<img>` 元素，其方式与显示任何图像的方式相同。</span><span class="sxs-lookup"><span data-stu-id="9ffff-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="9ffff-223">但是，`<img>` 元素不是引用 *.jpg*或 *.png*文件，而是引用包含创建图表的 `Chart` 帮助器的*cshtml 文件。*</span><span class="sxs-lookup"><span data-stu-id="9ffff-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9ffff-224">当显示页运行时，`<img>` 元素将获取 `Chart` 帮助器的输出，并呈现图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="9ffff-225">创建名为*ShowChart*的文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="9ffff-226">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="9ffff-227">代码使用 `<img>` 元素显示你之前在*ChartArrayBasic*文件中创建的图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="9ffff-228">在浏览器中运行网页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-228">Run the web page in a browser.</span></span> <span data-ttu-id="9ffff-229">*ShowChart*文件根据*ChartArrayBasic*文件中包含的代码显示图表图像。</span><span class="sxs-lookup"><span data-stu-id="9ffff-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="9ffff-230">设置图表样式</span><span class="sxs-lookup"><span data-stu-id="9ffff-230">Styling a Chart</span></span>

<span data-ttu-id="9ffff-231">`Chart` 帮助器支持大量选项，使您可以自定义图表的外观。</span><span class="sxs-lookup"><span data-stu-id="9ffff-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="9ffff-232">可以设置颜色、字体、边框等。</span><span class="sxs-lookup"><span data-stu-id="9ffff-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="9ffff-233">若要自定义图表的外观，一种简单的方法是使用*主题*。</span><span class="sxs-lookup"><span data-stu-id="9ffff-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="9ffff-234">主题是信息的集合，用于指定如何使用字体、颜色、标签、调色板、边框和效果来呈现图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="9ffff-235">（请注意，图表样式不指示图表类型。）</span><span class="sxs-lookup"><span data-stu-id="9ffff-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="9ffff-236">下表列出了内置主题。</span><span class="sxs-lookup"><span data-stu-id="9ffff-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="9ffff-237">主题</span><span class="sxs-lookup"><span data-stu-id="9ffff-237">Theme</span></span> | <span data-ttu-id="9ffff-238">说明</span><span class="sxs-lookup"><span data-stu-id="9ffff-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="9ffff-239">在白色背景上显示红色列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="9ffff-240">显示蓝色渐变背景上的蓝色列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="9ffff-241">显示绿色渐变背景上的蓝色列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="9ffff-242">在黄色渐变背景上显示橙色列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="9ffff-243">在白色背景上显示三维红列。</span><span class="sxs-lookup"><span data-stu-id="9ffff-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="9ffff-244">您可以指定在创建新图表时要使用的主题。</span><span class="sxs-lookup"><span data-stu-id="9ffff-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="9ffff-245">创建名为*ChartStyleGreen*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="9ffff-246">将页面中的现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="9ffff-247">此代码与使用数据库作为数据的前面的示例相同，但在创建 `Chart` 对象时将添加 `theme` 参数。</span><span class="sxs-lookup"><span data-stu-id="9ffff-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="9ffff-248">下面显示了更改后的代码：</span><span class="sxs-lookup"><span data-stu-id="9ffff-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="9ffff-249">在浏览器中运行页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-249">Run the page in a browser.</span></span> <span data-ttu-id="9ffff-250">您看到的数据与以前相同，但图表看起来更精美：</span><span class="sxs-lookup"><span data-stu-id="9ffff-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="9ffff-251">保存图表</span><span class="sxs-lookup"><span data-stu-id="9ffff-251">Saving a Chart</span></span>

<span data-ttu-id="9ffff-252">当您在本文中看到到目前为止，使用 `Chart` 帮助程序时，帮助程序将在每次调用时从头开始重新创建图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="9ffff-253">必要时，图表的代码还会重新查询数据库或重新读取 XML 文件以获取数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="9ffff-254">在某些情况下，执行此操作可能是一项复杂的操作，例如，如果要查询的数据库很大，或者 XML 文件包含大量数据。</span><span class="sxs-lookup"><span data-stu-id="9ffff-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="9ffff-255">即使图表不涉及大量数据，动态创建映像的过程也会占用服务器资源，如果很多人请求显示图表的页面，可能会对网站的性能产生影响。</span><span class="sxs-lookup"><span data-stu-id="9ffff-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="9ffff-256">为了帮助您降低创建图表的潜在性能影响，您可以在第一次需要图表时创建图表，并保存它。</span><span class="sxs-lookup"><span data-stu-id="9ffff-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="9ffff-257">如果再次需要图表，而不是重新生成它，则可以只提取保存的版本并呈现。</span><span class="sxs-lookup"><span data-stu-id="9ffff-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="9ffff-258">可以通过以下方式保存图表：</span><span class="sxs-lookup"><span data-stu-id="9ffff-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="9ffff-259">在计算机内存（在服务器上）缓存图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="9ffff-260">将该图表保存为图像文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="9ffff-261">将图表另存为 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-261">Save the chart as an XML file.</span></span> <span data-ttu-id="9ffff-262">此选项允许您在保存图表之前修改图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="9ffff-263">缓存图表</span><span class="sxs-lookup"><span data-stu-id="9ffff-263">Caching a Chart</span></span>

<span data-ttu-id="9ffff-264">创建图表后，可以对其进行缓存。</span><span class="sxs-lookup"><span data-stu-id="9ffff-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="9ffff-265">缓存图表意味着无需重新创建它，如果需要再次显示它。</span><span class="sxs-lookup"><span data-stu-id="9ffff-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="9ffff-266">将图表保存到缓存中时，请为其指定一个必须对该图表唯一的键。</span><span class="sxs-lookup"><span data-stu-id="9ffff-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="9ffff-267">如果服务器内存不足，则可能会删除保存到缓存中的图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="9ffff-268">此外，如果应用程序出于任何原因而重新启动，则会清除缓存。</span><span class="sxs-lookup"><span data-stu-id="9ffff-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="9ffff-269">因此，使用缓存的图表的标准方法是始终首先检查它是否在缓存中可用，如果没有，则创建或重新创建它。</span><span class="sxs-lookup"><span data-stu-id="9ffff-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="9ffff-270">在网站的根目录中，创建一个名为 " *ShowCachedChart*" 的文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="9ffff-271">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="9ffff-272">`<img>` 标记包含指向*ChartSaveToCache*文件的 `src` 属性，并将一个键作为查询字符串传递到页面。</span><span class="sxs-lookup"><span data-stu-id="9ffff-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="9ffff-273">项包含 &quot;myChartKey&quot;的值。</span><span class="sxs-lookup"><span data-stu-id="9ffff-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="9ffff-274">*ChartSaveToCache*文件包含创建图表的 `Chart` 帮助程序。</span><span class="sxs-lookup"><span data-stu-id="9ffff-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9ffff-275">稍后你将创建此页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="9ffff-276">页面末尾有一个指向名为*ClearCache*的页面的链接。</span><span class="sxs-lookup"><span data-stu-id="9ffff-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="9ffff-277">这是你稍后将创建的一个页面。</span><span class="sxs-lookup"><span data-stu-id="9ffff-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="9ffff-278">仅需*ClearCache*来测试此示例的缓存，而不是在处理缓存图表时通常会包含的链接或页面。</span><span class="sxs-lookup"><span data-stu-id="9ffff-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="9ffff-279">在网站的根目录中，创建一个名为*ChartSaveToCache*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="9ffff-280">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="9ffff-281">代码首先检查是否有任何内容作为键值传递到查询字符串中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="9ffff-282">如果是这样，则代码会尝试通过调用 `GetFromCache` 方法并向其传递密钥来读取缓存中的图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="9ffff-283">如果事实证明，该密钥下的缓存中没有任何内容（这会在第一次请求图表时出现），则代码会照常创建图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="9ffff-284">图表完成后，代码通过调用 `SaveToCache`将其保存到缓存中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="9ffff-285">该方法需要一个键（以便以后可以请求图表）和在缓存中保存图表的时间量。</span><span class="sxs-lookup"><span data-stu-id="9ffff-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="9ffff-286">（缓存图表所需的确切时间取决于您认为数据所代表的数据可能发生变化的频率。）如果将此参数设置为 true， &#8212;则 `SaveToCache` 方法还需要 `slidingExpiration` 参数，每次访问该图表时都会重置超时计数器。</span><span class="sxs-lookup"><span data-stu-id="9ffff-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="9ffff-287">在这种情况下，它实际上是指在用户上一次访问图表后，图表的缓存条目将在2分钟后过期。</span><span class="sxs-lookup"><span data-stu-id="9ffff-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="9ffff-288">（可调过期的替代方法是绝对过期，这意味着缓存项在放入缓存后会刚好在2分钟后过期，无论其访问频率如何。）</span><span class="sxs-lookup"><span data-stu-id="9ffff-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="9ffff-289">最后，代码使用 `WriteFromCache` 方法从缓存中提取并呈现图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="9ffff-290">请注意，此方法不在检查缓存的 `if` 块的外部，因为它将从缓存中获取图表，无论图表是以哪种方式开始，还是必须在缓存中生成并保存。</span><span class="sxs-lookup"><span data-stu-id="9ffff-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="9ffff-291">请注意，在此示例中，`AddTitle` 方法包含时间戳。</span><span class="sxs-lookup"><span data-stu-id="9ffff-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="9ffff-292">（将当前日期和时间&#8212; `DateTime.Now` &#8212;添加到标题中。）</span><span class="sxs-lookup"><span data-stu-id="9ffff-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="9ffff-293">创建名为*ClearCache*的新页面，并将其内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="9ffff-294">此页使用 `WebCache` 帮助器删除*ChartSaveToCache*中缓存的图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="9ffff-295">如前文所述，您通常不需要像下面这样的页面。</span><span class="sxs-lookup"><span data-stu-id="9ffff-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="9ffff-296">此处只需创建它，以便更轻松地测试缓存。</span><span class="sxs-lookup"><span data-stu-id="9ffff-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="9ffff-297">在浏览器中运行*ShowCachedChart*网页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="9ffff-298">页面根据*ChartSaveToCache*文件中包含的代码显示图表图像。</span><span class="sxs-lookup"><span data-stu-id="9ffff-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="9ffff-299">记录时间戳在图表标题中显示的内容。</span><span class="sxs-lookup"><span data-stu-id="9ffff-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![说明：图表标题中带有时间戳的基本图表图片](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="9ffff-301">关闭浏览器。</span><span class="sxs-lookup"><span data-stu-id="9ffff-301">Close the browser.</span></span>
8. <span data-ttu-id="9ffff-302">再次运行*ShowCachedChart* 。</span><span class="sxs-lookup"><span data-stu-id="9ffff-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="9ffff-303">请注意，时间戳与以前相同，这指示图表不会重新生成，而是从缓存中读取。</span><span class="sxs-lookup"><span data-stu-id="9ffff-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="9ffff-304">在*ShowCachedChart*中，单击 "**清除缓存**" 链接。</span><span class="sxs-lookup"><span data-stu-id="9ffff-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="9ffff-305">这会将你转到*ClearCache*，该操作报告缓存已清除。</span><span class="sxs-lookup"><span data-stu-id="9ffff-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="9ffff-306">单击 "**返回 ShowCachedChart** " 链接，或从 WebMatrix 重新运行*ShowCachedChart* 。</span><span class="sxs-lookup"><span data-stu-id="9ffff-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="9ffff-307">请注意，此时间戳已更改，因为缓存已被清除。</span><span class="sxs-lookup"><span data-stu-id="9ffff-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="9ffff-308">因此，该代码必须重新生成图表并将其放回到缓存中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="9ffff-309">将图表保存为图像文件</span><span class="sxs-lookup"><span data-stu-id="9ffff-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="9ffff-310">你还可以将图表保存为服务器上的图像文件（例如， *.jpg*文件）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="9ffff-311">然后，你可以像使用任何映像一样使用映像文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="9ffff-312">其优势在于存储文件，而不是保存到临时缓存中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="9ffff-313">您可以在不同的时间（例如，每小时）保存一个新的图表图像，然后保留一段时间内发生的更改的永久记录。</span><span class="sxs-lookup"><span data-stu-id="9ffff-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="9ffff-314">请注意，您必须确保您的 web 应用程序有权将文件保存到服务器上要放置映像文件的文件夹中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9ffff-315">在网站的根目录中，创建一个名为 *\_ChartFiles*的文件夹（如果它尚不存在）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="9ffff-316">在网站的根目录中，创建一个名为*ChartSave*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="9ffff-317">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="9ffff-318">代码首先通过调用 `File.Exists` 方法来查看 *.jpg*文件是否存在。</span><span class="sxs-lookup"><span data-stu-id="9ffff-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9ffff-319">如果该文件不存在，则代码将从数组创建新 `Chart`。</span><span class="sxs-lookup"><span data-stu-id="9ffff-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="9ffff-320">这一次，代码调用 `Save` 方法，并传递 `path` 参数以指定保存图表的位置的文件路径和文件名。</span><span class="sxs-lookup"><span data-stu-id="9ffff-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="9ffff-321">在页面的正文中，`<img>` 元素使用指向要显示的 *.jpg*文件的路径。</span><span class="sxs-lookup"><span data-stu-id="9ffff-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="9ffff-322">运行*ChartSave*文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="9ffff-323">返回到 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="9ffff-323">Return to WebMatrix.</span></span> <span data-ttu-id="9ffff-324">请注意，名为*chart01*的图像文件已保存在 *\_ChartFiles*文件夹中。</span><span class="sxs-lookup"><span data-stu-id="9ffff-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="9ffff-325">将图表另存为 XML 文件</span><span class="sxs-lookup"><span data-stu-id="9ffff-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="9ffff-326">最后，可以将图表保存为服务器上的 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="9ffff-327">使用此方法对图表进行缓存或将图表保存到文件的一个优点是，可以在显示图表之前修改 XML （如果需要）。</span><span class="sxs-lookup"><span data-stu-id="9ffff-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="9ffff-328">您的应用程序必须对要放置映像文件的服务器上的文件夹具有读/写权限。</span><span class="sxs-lookup"><span data-stu-id="9ffff-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9ffff-329">在网站的根目录中，创建一个名为*ChartSaveXml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="9ffff-330">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="9ffff-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="9ffff-331">此代码类似于您之前在缓存中存储图表所看到的代码，只不过它使用 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="9ffff-332">代码首先通过调用 `File.Exists` 方法来检查 XML 文件是否存在。</span><span class="sxs-lookup"><span data-stu-id="9ffff-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9ffff-333">如果该文件存在，则代码会创建一个新的 `Chart` 对象，并将该文件名作为 `themePath` 参数传递。</span><span class="sxs-lookup"><span data-stu-id="9ffff-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="9ffff-334">这将根据 XML 文件中的内容创建图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="9ffff-335">如果 XML 文件尚不存在，则代码会创建一个图表，如 normal，然后调用 `SaveXml` 将其保存。</span><span class="sxs-lookup"><span data-stu-id="9ffff-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="9ffff-336">如之前所见，使用 `Write` 方法呈现图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="9ffff-337">与显示缓存的页面一样，此代码在图表标题中包含时间戳。</span><span class="sxs-lookup"><span data-stu-id="9ffff-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="9ffff-338">创建名为*ChartDisplayXMLChart*的新页面，并向其添加以下标记：</span><span class="sxs-lookup"><span data-stu-id="9ffff-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="9ffff-339">运行*ChartDisplayXMLChart*页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="9ffff-340">将显示图表。</span><span class="sxs-lookup"><span data-stu-id="9ffff-340">The chart is displayed.</span></span> <span data-ttu-id="9ffff-341">记下图表标题中的时间戳。</span><span class="sxs-lookup"><span data-stu-id="9ffff-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="9ffff-342">关闭浏览器。</span><span class="sxs-lookup"><span data-stu-id="9ffff-342">Close the browser.</span></span>
6. <span data-ttu-id="9ffff-343">在 WebMatrix 中，右键单击 *\_ChartFiles* "文件夹，单击"**刷新**"，然后打开该文件夹。</span><span class="sxs-lookup"><span data-stu-id="9ffff-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="9ffff-344">此文件夹中的*XMLChart*文件是由 `Chart` 帮助程序创建的。</span><span class="sxs-lookup"><span data-stu-id="9ffff-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![说明： _ChartFiles 文件夹，其中显示了由图表帮助器创建的 XMLChart 文件。](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="9ffff-346">再次运行*ChartDisplayXMLChart*页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="9ffff-347">图表显示的时间戳与首次运行页面时的时间戳相同。</span><span class="sxs-lookup"><span data-stu-id="9ffff-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="9ffff-348">这是因为，该图表是从之前保存的 XML 生成的。</span><span class="sxs-lookup"><span data-stu-id="9ffff-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="9ffff-349">在 WebMatrix 中，打开 *\_ChartFiles*文件夹并删除*XMLChart*文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="9ffff-350">再次运行*ChartDisplayXMLChart*页。</span><span class="sxs-lookup"><span data-stu-id="9ffff-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="9ffff-351">此时，时间戳会更新，因为 `Chart` 帮助程序必须重新创建 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="9ffff-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="9ffff-352">如果需要，请检查 *\_ChartFiles* "文件夹，并注意该 XML 文件已返回。</span><span class="sxs-lookup"><span data-stu-id="9ffff-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="9ffff-353">其他资源</span><span class="sxs-lookup"><span data-stu-id="9ffff-353">Additional Resources</span></span>

- [<span data-ttu-id="9ffff-354">使用 ASP.NET 网页站点中的数据库简介</span><span class="sxs-lookup"><span data-stu-id="9ffff-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="9ffff-355">在 ASP.NET 网页站点中使用缓存来提高性能</span><span class="sxs-lookup"><span data-stu-id="9ffff-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="9ffff-356">[Chart 类](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99))（MSDN 上的 ASP.NET 网页 API 参考）</span><span class="sxs-lookup"><span data-stu-id="9ffff-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
